# HuggingFace：Common Voice 22 为 gated 数据集，先在
# https://huggingface.co/datasets/mozilla-foundation/common_voice_22_0 页面同意条款，
# 再填入有权限的 token；同时用于上传模型/数据集
HF_TOKEN=

# 国内网络建议启用镜像
# HF_ENDPOINT=https://hf-mirror.com
# 走镜像时 paths.py 会自动设 HF_HUB_DISABLE_XET=1（镜像不代理 xet 的 CAS 服务器，
# 不关会在 reconstruction 阶段直连并报 401）；要强制用 xet 就显式设成 0

# wandb 监控（可选，不填则跳过桥接）
WANDB_API_KEY=
WANDB_PROJECT=voxcpm-finetune

# 基座模型（不填则默认 openbmb/VoxCPM2）
VOXCPM_BASE_PATH=

# GPU 服务器上把大数据目录指到大盘（如 autodl）；不填默认项目内 ./data、./checkpoints
# VOXFT_DATA_ROOT=/root/autodl-tmp/voxft_data
# VOXFT_CKPT_ROOT=/root/autodl-tmp/voxft_ckpt

# HF 缓存（基座模型+数据集分片）默认 ~/.cache/huggingface，系统盘小的机器指到大盘
# （命令行手动下载需另行 export，hf CLI 不读本文件）
# HF_HOME=/root/autodl-tmp/hf_home

# Whisper 权重覆盖（可填本地目录，或镜像上的仓库名）；不填走 faster-whisper 默认下载
# large-v3 约 3GB，国内直连 huggingface.co 常在 SSL 握手就超时，务必配好 HF_ENDPOINT
# VOXFT_WHISPER_MODEL=medium
# VOXFT_WHISPER_MODEL_LARGE=/root/autodl-tmp/hf_home/faster-whisper-large-v3
