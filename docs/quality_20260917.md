# 无 drama 质量实验：2026-09-17

## 边界和当前状态

- 不改生产 r8，不修改官方 submodule，不下载新语料、不启动新训练。
- 声学异常与母语评分已拆分；未评/不确定保留为空，不算干净、平局或通过。
- `runlog` 三个入口与 CLI 的 CER 默认阈值统一为 `0.05`，可显式覆盖；历史数字未改。
- `voxft.eval --summarize` 从已有逐条指标另存稳定性报告，不重合成、不覆盖人工标注。
- `voxft.eval --lora-strength` 额外缩放严格加载的 LoRA 增量，退出生成（包括异常）恢复原强度。
- 新增回归文件 `eval_cases/acoustic_regressions_20260917.jsonl`；旧 MFCC SIM 不与新 WavLM SIM 比较。
- **本轮已结束**：480 条强度对照 + 210 条参数对照共 **690 条**，另有8条端点校验音频；
  GPU 合成于 2026-09-17 14:46:40（+08:00）完成。最终本地 **121 项**、远端 **108 项**测试通过，
  分别对应两端当前测试集；GPU 合成任务已退出。
- **没有候选通过本轮工程筛查，不升级生产**。保留 r8、strength1、CFG1.8、steps20；
  没有新增训练/merge/HF 上传。口音、语言自然度与情绪仍未验证。

## 既有完整 v2 报告：先看方差，而非只看均值

同一 182 case × seed 42–46；CFG=1.8、20 steps、禁自动重试。
每 case 先算样本标准差（ddof=1），再按语种对 case 等权取均值。

| 语种 | SIM std base→r8 | 时长 std 秒 base→r8 | CER std base→r8 |
|---|---|---|---|
| th | .019363 → .020950 | .513141 → .404292 | .060341 → .043844 |
| tl | .012612 → .012928 | .614631 → .501361 | .005214 → .008502 |
| vi | .022749 → .021527 | .423275 → .349442 | .033353 → .029188 |
| id | .022710 → .021033 | .525984 → .394957 | .024897 → .017886 |
| ms | .019940 → .017031 | .554198 → .476339 | .052662 → .006375 |
| zh | .013345 → .016033 | .467449 → .436303 | .040722 → .082793 |
| en | .019093 → .012548 | .340463 → .339883 | .006627 → .007427 |

**观察**：r8 的时长波动各语种都较小，但 SIM 波动在 th/tl/zh 较大；
zh CER 波动也较大，不能笼统宣布“更稳定”。SIM-to-ref 的 std 不是生成音频之间的音色距离，
因此另用 `scripts/voice_stability.py` 重算生成音频两两 WavLM 余弦：
同 case 跨 seed、同 ref 文件/同 seed/同语种跨 cue 分开；少于两条的组不作稳定性结论。
42–46 **没有覆盖 +7 重试**。

### 生成音频两两 WavLM 余弦（完整 1820 条音频重算已完成）

| 语种 | 同 case 跨 seed 均值 base→r8 | 同 ref/seed 跨 cue 均值 base→r8 |
|---|---|---|
| th | .965186 → .968574 | .938410 → .946657 |
| tl | .982245 → .982895 | .972115 → .971019 |
| vi | .964625 → .970407 | .943327 → .947209 |
| id | .959346 → .965007 | .936872 → .946677 |
| ms | .966065 → .973372 | .953914 → .964274 |
| zh | .973020 → .973220 | 无可配对组，不填 1 或“稳定” |
| en | .971407 → .973865 | 无可配对组，不填 1 或“稳定” |

跨 seed 均值所有语种均较好；这与 SIM-to-ref std 不完全一致，说明两者不能互相替代。
跨 cue 每个目标语种 65 个组（按 ref 文件/seed/语言分组，组内对不同文本两两比较）；
TL 均值略差，且**均值改善不能掩盖个别组明显变差**：

- `id_nat_11 / id_nat_24`，ref_zh_01、seed42：余弦 **.905397 → .778049**（Δ −.127348）。
- `tl_nat_09 / tl_nat_22`，ref_en_01、seed42：**.936560 → .841543**（Δ −.095017）。
- `id_nat_12 / id_nat_25`，ref_en_01、seed44：Δ −.064627；seed43：Δ −.062402。

这是自动音色一致性**诊断**，不是人工确认的娃娃音/音色跳变标签。
前两组共四条 case 纳入后续参数排查，不再算本轮未参与调参的验证样本。
完整分析保存在 `voxft_ckpt/eval/voice_stability/`，不覆盖输入报告。

原报告（大盘 `voxft_ckpt/eval/`）：

- base：`base_8de890a3af74491b948c1ae0449967b0.json`
- r8：`lora_omni5_r8_latest_f1315988633448b1bd151f4fd999323b.json`
- r9：`lora_omni5_r9_latest_a2f9e99f614245f0842c37a076c381bf.json`

## 强度 pilot（480 条生成与诊断已完成）

执行脚本：`scripts/quality_probe.py`，远端工作目录 `/root/autodl-tmp/quality_20260917`。

- 固定选择 seed=20260917，五个目标语种各随机选 zh/en/tl ref 的 case 各一条；
  每语种另选一条历史 CER 回退诊断 case；中文/英文回放各随机两条。共 **24 case**。
- 15 条随机目标语种对照、5 条诊断富集、4 条随机回放**分别汇总**；
  CER 富集样本不是人工确认的坏例，不能将其占比当线上坏例率。
- 强度 **0/.5/.75/1**，每档 24×5=120 条，全部重新合成，
  固定 text/ref/seed42–46、CFG1.8、steps20、禁自动重试；不拿不同执行结构的历史音频混作新端点。
- 先用两个分片的首 case 做真实 GPU 端点验证：
  **strength=0 与官方禁用 LoRA、strength=1 与官方启用 LoRA，WAV 采样逐点完全一致，四项均通过**。
- `plan.json` 保存选样、原报告 SHA256 和剩余 158 case。剩余样本不参与本轮 pilot 调参；
  它们是既有评测集的一部分，不宣称是从未看过的外部独立测试集。
- 两两音色一致性另行汇总；不根据单个 CER 均值选择生产参数。

| 语种 | strength0 CER | .5 | .75 | 1（r8） |
|---|---:|---:|---:|---:|
| th | .1457 | .1340 | .1661 | .1458 |
| tl | .0079 | .0134 | .0129 | .0137 |
| vi | .1261 | .1156 | .0932 | .1226 |
| id | .0465 | .0347 | .0374 | .0439 |
| ms | .0358 | .0156 | .0126 | .0109 |
| zh | .0600 | .0333 | .0267 | .0367 |
| en | .0119 | .0119 | .0044 | .0044 |

这张表混有诊断富集与数字 case，**只作诊断，不是线上坏例率或发音验收**。
按随机目标语种对照单列，`.75→1` 的 CER 为：
th `.1360→.1265`、tl `.0138→.0115`、vi `.0970→.1099`、
id `.0107→.0095`、ms `.0073→.0082`；并非各语种同向改善。

时长类门禁也不能跳过：

- `.5` 对 strength0：MS p90 尾静音 `.14→.26s`（+0.12s）触发警告。
- `.75` 对 strength0：ID `.16→.28s`、MS `.14→.26s`；
  对 strength1：EN `.32→.46s`（+0.14s）触发警告。
- strength1 本身在这组小样本上也有 ID 时长/尾静音、MS 尾静音及 VI 有声占比警告，
  不能拿富集子集直接推翻完整 v2 的既有生产决策，但也不能只拿有利指标为候选报“通过”。

**当前决定：不推荐全局降 LoRA 强度，不替换 r8。**
收益不单调且有语种/尾静音权衡；尚未得到值得直接推进转正的统一方案。
不把“ΔCER 未超过0.05”误写成已经证明无退化。所有分组原始数字在 `comparison.json` 与四份 `strength_*.json`。

四档生成音频的两两音色分析也已完成（同 case 跨 seed 余弦均值）：

| 语种 | strength0 | .5 | .75 | 1 |
|---|---:|---:|---:|---:|
| th | .979930 | .981629 | .982824 | .985436 |
| tl | .988703 | .986764 | .987819 | .988970 |
| vi | .956376 | .957751 | .961994 | .957219 |
| id | .957410 | .963129 | .964580 | .966190 |
| ms | .961885 | .965888 | .965479 | .971653 |

本 pilot 内 TH/TL/ID/MS 均以 strength1 较好，VI 以 .75 较好；再次否定“统一减半就更稳定”的假设。
pilot 跨 cue 只覆盖 ID/MS/TL 各一对文本×5 seed，覆盖不足，不能替代完整 v2 结果；
原始表在 `quality_20260917/voice_stability/strength_*.json`。这里的余弦不是母语自然度评分。

复现（仅远端；worker 一律另起 nohup 日志，见 AGENTS.md）：

```bash
.venv/bin/python scripts/quality_probe.py prepare --work /root/autodl-tmp/quality_new
.venv/bin/python scripts/quality_probe.py worker --work /root/autodl-tmp/quality_new --shard 0/2
.venv/bin/python scripts/quality_probe.py worker --work /root/autodl-tmp/quality_new --shard 1/2
.venv/bin/python scripts/quality_probe.py collect --work /root/autodl-tmp/quality_new
```

`prepare` 拒绝覆盖已有实验目录，worker 拒绝覆盖已完成分片；失败先排查日志，不盲目重跑。

### 单变量对照（210 条生成、聚合及音色分析均已完成）

保持**当前生产 r8 strength=1**，不把尚未验收的强度候选和参数同时修改：

1. CFG1.8 / steps20（重新跑同口径基线）。
2. CFG1.6 / steps20（只降低 CFG）。
3. CFG1.8 / steps28（只增加步数）。

取 pilot 随机对照中每语种最短的一条（五语种+zh/en，共7条），加入3条历史标注坏例、
两组4条跨 cue 诊断，去重后最多14条。每组固定 seed **42/43/44/45/49**，
覆盖 **42→43（+1）和42→49（+7）**。不同 cohort 分开看，不外推线上坏例率。
这里只筛诊断信号，不宣称母语质量改善；同人不同 ref 对照仍需可信替代 ref，不能拿不同人冒充。

`scripts/quality_probe.py followup --shard 0/2` 与 `1/2` 各写自己的条件快照，
后台 `followup.sh` 等两片完成、核对两份快照完全一致后才合并；失败不自动重试或替换模型。
`gen_sec` 来自并行实验，受 GPU 争用影响，不能当独占生产延迟的 A/B 结论。

| 语种 | CFG1.8 / 20步 CER | CFG1.6 / 20步 | CFG1.8 / 28步 |
|---|---:|---:|---:|
| th | .0702 | .0764 | .0702 |
| tl | .0307 | .0423 | .0340 |
| vi | .0000 | .0000 | .0000 |
| id | .0379 | .2469 | .0525 |
| ms | .0167 | .0229 | .0179 |
| zh | .0400 | .0800 | .0667 |
| en | .0000 | .0000 | .0000 |

**CFG1.6 不推荐作为全局默认**：`id_digit_3 / seed42` 从 4.96s 延长至 **22.72s**，
CER `.0294→4.7059`、ASR 长度比 `1→5.647`，触发多读诊断。ID 非数字 CER
`.0074→.0057` 却近似不变，说明问题集中在这个数字压力例，而非普遍发音退化。
具体条件已写入 `eval_cases/parameter_regressions_20260917.jsonl`，没有伪装成人工标注。

**28步也未获得统一收益**：EN p90 尾静音 `.22→.36s`（+0.14s）触发门禁；
诊断组的跨 cue 音色一致性没有改善：

| 同 ref/seed 的诊断文本对 | 1.8 / 20 | 1.6 / 20 | 1.8 / 28 |
|---|---:|---:|---:|
| ID 两两余弦均值（5 seed） | .922199 | .915081 | .915680 |
| TL 两两余弦均值（5 seed） | .907494 | .876431 | .885208 |

这些是小规模诊断对照，不是外部独立验收；也**没有测试 CFG1.6+28步的联合设置**，
不能据此否定生产中按明确金属音触发的联动重试。当前不扩大网格、不选最好的一条宣称修复，
也不因 GPU 可用就启动缺证据的训练。

最终6006声学审阅报告（均同14 case/同5 seed，甲乙可随机；未评项仍为空）：

- A 基线：`r8_cfg1.8_steps20_followup_20260917_1e3f9c5b38724eb7adf090b905a261ed.json`
- B 降CFG：`r8_cfg1.6_steps20_followup_20260917_2e1db2e611d24a1380441659e9e9f713.json`
- B 加步数：`r8_cfg1.8_steps28_followup_20260917_00181f1ac3d3443a89a920c544a05424.json`

报告在大盘 `voxft_ckpt/eval/`；派生汇总位于 `quality_20260917/followup_comparison.json`。
原始文件不覆盖，本地只回传 JSON 留档，不回传语料或音频。

### 本轮发现并修复的口径错误

原 `duration_inflation` 将**全量** `mean_audio_sec` 配**非数字** CER，
会把上述数字跑飞错写成“内容没变但变长”。现两者都取同一非数字子集，
没有调整阈值：CFG1.6 的错误时长归因消失，但 ID 内容红线和22.72s长输出诊断仍保留。
回测旧 r2：非数字时长 `2.3238→2.6667s`、CER `.0022→.0030`，仍能检出原有约15%的时长问题。
新增字段同时进入 overall/by_lang/merge；无字段的旧报告该项未评，先 `--summarize` 另存。
修复前派生汇总留为 `followup_comparison_before_duration_fix.json`，不改历史逐条指标；
`--summarize` 也保留原始说明，不能把旧 MFCC 测量重新标成 WavLM。

## 历史明确坏例及发生阶段

来自旧 `base_87eed5ce…` 的人工字段，不从自然度分倒推母语结论：

| case/seed | 历史标注 | 自动诊断与范围 |
|---|---|---|
| th_prod_1 / 43 | noise=true | CER=0、metallic=false，自动规则漏掉；准确类型/时间点未记 |
| ms_manglish / 43 | cutoff=true | suspected_truncation=false；时间点未记 |
| id_digit_3 / 44 | cutoff=true | 含数字，CER 受转写归一化影响；时间点未记 |

三条均已存在于**模型裸输出**，证明这些特定异常不必由生产后处理引入；
尚无同 text/ref/seed 的“裸输出→生产处理后”成对产物，不能据此豁免全部后处理。
坏例回归集与随机对照分开；“异常娃娃音”尚无带定位的已确认样本，不按高 F0 伪造标签。

## 现有语料审计：同源切片路线尚不具备完整输入

审计直接读取远端已有清单，未修改任何训练源。这里只统计 >=8 秒候选，不等于可安全切分。

| 源 | >=8 秒条数 | 已验证 speaker 条数 | 已存词级对齐 |
|---|---:|---:|---:|
| fleurs_th_tc | 1707 | 0 | 0 |
| fleurs_tl_tc | 1525 | 0 | 0 |
| fleurs_vi_tc | 1614 | 0 | 0 |
| fleurs_id_tc | 1773 | 0 | 0 |
| fleurs_ms_tc | 1927 | 0 | 0 |
| cv22_th | 454 | 454 | 0 |
| cv22_id | 65 | 65 | 0 |
| cv22_vi | 0 | 0 | 0 |

FLEURS 现有清单没有可信全库身份，也未登记片内单人审核证据或词级对齐。
**不能将“长录音”自动当作“已确认单人且可切出正确 target 文本”**。
CV22 th/id 已有可信身份，应优先复用现有跨文件配对，而非为了实验发明同源配对绕过。
局部单人证据可另审计，但不伪装成全库 speaker ID；不放松 `pair_references` 的同 origin 守卫。

结论：没有因 GPU 已准备好就启动缺证据的 ref 切片训练；先完成无需新数据的强度/参数实验。
完整计数与候选元数据见远端 `data_audit.json`，原始文件与旧清单未变。
