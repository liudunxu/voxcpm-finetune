# 2026-09-20 新ID/VN成片链路复核

本轮检查用户新增ID `mtbup5vzqs5d4a` 和VN `mtbv0gkefabl3c`。
与此前1:50反馈的 `mtbtg7qhj9j529` 是不同产物，旧记录保持原样。
详细上下游定位、修复与回归见DIS
`docs/reviews/2026-09-20-id-vn-reference-and-tail.md`。

## ID

- 内嵌生成时间为2026-09-20 10:40:28北京时间，42cue；
  10条显式保留原声、32条拟合成，末句失败，其余31条TTS出片。
- **末句159.20–161.48不是没翻译**：「Kamu, Apa yang kau pegang?」已进入合成，
  但超读候选在最终双ASR质检失败后保留原声。较完整的1.81s候选仍有发声风险，
  6.79s候选则由5词增为14词；两者都不是全面通过。
- 可复现排序bug：`extraneous tail speech`未被共用内容污染分类识别，
  遗漏既有400分惩罚；冻结两候选QC旧评分−484.486/−286.188，
  修后−484.486/−686.188。未改评分权重、未取消最终内容门，不保证第一份最终能出片。
- **36.66–37.86（cue8）**用户听到男变女；短参考1.757s、有效语音约0.921s、
  SNR2.63dB、无合格同人锚点。采纳候选性别未知，被拒候选才带高置信female标签。
  不把被拒候选的自动标签拿来证明出片性别。
- **77–85秒、91–94秒**另有两条`gender_drift_shipped`，属于待试听风险；
  **41.8–43.76、52.98–54.18秒**有已证实的超窗硬裁和未完成refit，优先核对上下游。
  最终共7条TTS带severe；自动标签不等于7条人工坏例。

## VN

- 生成时间2026-09-20 11:06:36北京时间；14条全TTS，没有原声回退、无时间窗硬裁记录。
  但1/4/5/6共4条仍带severe，另8/12/13也待复核，不能称已验收。
- **14.68–19.34秒（4/5）**共用弱锚点，参考SNR0.56dB，有效语音占比.582，
  两条身份不匹配仍出片；**19.34–22.44秒（6）**脏参考SNR0.24dB，三句短语挤在1.62s。
  文本能找齐不证明音色/节奏合格。
- **65.16秒（13）**先因音区冲突拒cue14锚点，弱参考救援却又借回cue14。
  已复现为身份保护缺口，不凭390/122Hz推定真实角色性别。
- **24.34秒（7）**三词短句最终3.38s，语音到27.645秒，下一cue30秒；
  有尾部余量且未硬裁，但节奏仍值得听。开头粗源边界为1.32s、合成放0s，
  只是起点风险线索，不全局移动音轨。
- `source_language_code=en`，但12条源文为中文、两条“Thank you.”。
  需要核对真正原声/字幕来源，不能凭中文源文把音频源语强改zh。
- 发现锚点预处理早于逐cue元数据及安全参考窗准备，canonical缓存拿到目标译文/
  临时speaker及未加载的风险标记。现调整调用顺序，不添加新模型调用。
  主VoxCPM请求另有自己的prompt逻辑，不把meta文字异常冒充实际错发prompt证据。

## 实施与边界

DIS本地四项最小修复：尾部超读分类、控制指令完整短语预算、锚点准备顺序、
身份冲突下禁止弱参考救援按不可信speaker借音。保留此前keep-best音区/静音保护。
本轮新回归旧实现6失败/2健康对照通过；首两修复357项及VN相关138项通过，
最终整合446项通过，应用实际DIS目录后84项复核通过；各轮范围重叠、不相加。
DIS已随`aebf175`推送，原有改动保留。无新GPU/LLM/ASR调用，无新训练、部署或听评，
没有提高重试预算/修改全局阈值；不承诺端到端耗时零变化。

证据：`checkpoints/video_review_20260920_mtbup5v/`、
`checkpoints/video_review_20260920_vn_mtbv0gk/`（ignored）。
完整meta/视频和失败证据不入Git，不把模型成片混入真人训练语料。
缺原始take/ref与任务音频包描述符，尚不能验证声学收益；没有为缺材料重抽或猜内部下载接口。
后续先做链路上线核对和已有音频局部复听，再按稳定坏例决定是否需要同条件base/r8对照。
**保持r8和暂停训练的边界，语言口音/自然度仍未验证。**
