# r11：同源 TL 短句抽样小实验

日期：2026-09-18。**生产仍保持 r8；本轮是覆盖假设实验，不是已证实的坏例修复。**
不续训r8，不扩大r10 ref路线，不等待drama、母语评分或线上身份追踪。

## 结果与决策：990条已完成，不转正、不扩训

**2026-09-18 10:20:18（北京时间）**，840条短句和150条五语种冒烟全部生成、评分并落盘，
六份报告通过完整case/seed/参数/元数据和WAV存在性校验，`done`已写入。
原控制器与接管控制器均已退出，**不要恢复PID13835或重复提交任何评测/训练**。
`pilot_audio_complete_not_accepted`表示实验完成，不是质量验收通过。

结论：**本轮没有得到支持TL短录音份额7.98%→25.03%的明确收益，不扩大此剂量、不加epoch、
不替换r8，也不为候选追加全量评测。** 这不证明所有短句数据路线无效。

### TL短句：同14条文本、三ref、五seed

| 模型 | 音频数 | CER | WER | 均时长s | 尾静音p90s | 有声占比 | 对ref相似度 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| base | 210 | 0.0156 | 0.0732 | 4.5691 | 0.20 | 0.9191 | 0.9137 |
| r8 | 210 | 0.0215 | 0.0909 | 4.8693 | 0.30 | 0.8832 | 0.9147 |
| A：原抽样 | 210 | 0.0250 | 0.1045 | 4.8770 | 0.36 | 0.8766 | 0.9267 |
| B：短句增配 | 210 | 0.0246 | 0.0974 | 4.9813 | 0.36 | 0.8728 | 0.9265 |

`scripts/short_cue_results.py`复用既有配对校验，先平均同条件五seed，再平均同文本的ref条件，
按`(lang, source_sentence_id)`的**14条独立文本**做5000次配对bootstrap，RNG20260917：

| B−A指标 | 均差 | 文本聚类95%区间 |
| --- | ---: | --- |
| CER | −0.000472 | [−0.003751, +0.002768] |
| WER | −0.007089 | [−0.017411, +0.003142] |
| 音频时长 | +0.104381s | [+0.060190, +0.144762]s |
| 对ref相似度 | −0.000237 | [−0.002731, +0.001927] |

- 内容指标区间跨零，B反而稍长，不能说短句增配改善了配音。
  B−r8的CER为+0.003023，区间[−0.002155,+0.009261]，也没有优于r8的明确内容信号。
- A/B相对r8的ref相似度均上升，但两组之间无明确差异；**这是输出对ref相似度，
  不是生成音频之间的跨seed/跨cue音色稳定性**，本轮未追加两两音色计算。
- 沿用原门禁：A/B相对base的尾静音p90均0.20→0.36s，增量0.16s，触发相对尾静音风险。
  相对r8及B相对A没有新增时长类红线；未越线不等于等效或质量通过。
- r8在这14句的CER也比base高0.005902，条件区间[+0.001617,+0.010640]；
  记录为较短TL朗读文本的局部风险，不用其它语种收益掩盖，也不据此推翻此前完整留出或自动回滚。
- 疑似漏尾：base/A/B均0/210，r8为2/210；均是ASR启发式，不是人工确认的截断。
  14句仍为57–80字符的朗读文本，不代表充分覆盖1–3秒对白。

区间条件于当前ref、seed与已训练模型，不含训练seed重复、说话人总体不确定性或多重比较校正。
所有样本非数字，时长与CER取同批；缺失指标整句排除，不把缺失当0。

### 五语种冒烟：每语种仅3句×5seed

base/r8各75条从原留出报告按相同case/seed精确复用，原报告SHA256与冻结计划一致；
不是拿原750条全量均值与本轮75条比较。

| 语种 | base CER | r8 CER | A CER | B CER |
| --- | ---: | ---: | ---: | ---: |
| TH | 0.0219 | 0.0344 | 0.0320 | 0.0341 |
| TL | 0.0540 | 0.0156 | 0.0217 | 0.0363 |
| VI | 0.0211 | 0.0191 | 0.0173 | 0.0161 |
| ID | 0.0036 | 0.0000 | 0.0073 | 0.0060 |
| MS | 0.0022 | 0.0065 | 0.0088 | 0.0042 |

- B对A或r8均未越过ΔCER>0.05的红线，但TL的B−A为+0.014587，
  三文本区间[−0.010440,+0.055240]；样本不足，不声称五语种无退化。
- A相对base在TH/VI/ID/MS、B在五语种均触发尾静音p90相对风险；
  r8在这小样本中的TH/TL/ID也触发。相对r8及B−A均无新增时长类红线。
  没有触发非数字时长膨胀或有声占比门禁，不能把上述尾静音直接升级成“整句拖长缺陷已定案”。
- B的MS `fleurs_test_ms_1939/seed49`触发一次疑似漏尾；原ASR保留目标句尾后多出`dalam`，
  尚未确认真实截断，不改冻结启发式或删除该条。
- 完整五语种留出、中英文回放、旧数字/坏例压力以及本轮声学盲听未验收；
  口音、自然度与情绪均**未验证**。本轮没有收益信号，不继续堆候选评测预算。

### 两条中文转写异常：24次原WAV复核已完成

10:27完成，仅复用12条已有WAV：`r11_tl_1902_zh`在seed42/44的四模型共8条，
再固定随机取`r11_tl_1778_zh/seed42`四模型作对照（RNG20260918，只看元数据、不排除重合）。
两组解码均temperature=0、VAD开启，唯一差异是自动检测语言或指定TL，共24次ASR；没有新TTS。

| 原音频 | 原CER / 零温度auto CER | auto识别语言 | 指定TL CER |
| --- | ---: | --- | ---: |
| A / 1902_zh / seed44 | 0.5893 / 0.5893 | zh | 0.0000 |
| B / 1902_zh / seed42 | 0.7857 / 0.7857 | zh | 0.1071 |

另10条CER不变，四条随机对照均为0。说明这两条分数明显受语言解码选择影响，
**不能仅凭中文转写认定TTS在讲中文，也不能反过来把指定TL低分当作发音正确**。
指定语言返回的概率1不是独立语言鉴定；每配置只解码一次，不声称重复性已全面验证。
不改全局ASR默认、不挑低分替换原报告，以上所有正式统计仍用原始分数；
未追加全量ASR、WavLM或试听包。

### 产物、检查与下一步

- 正式报告与配对结果：大盘`short_cue_ab_20260918/eval_recovery_20260918/`，
  新增`paired_results.json`；本机`checkpoints/`下同名路径已备份。
  原始990条结果、失败证据、训练权重与输入均保留。
- ASR诊断计划/逐条结果在同目录`asr_language_probe/`，源码归档`asr_language_probe.py`；
  含原报告/WAV/模型/评分源码指纹，只复用已缓存权重，没有数据集下载。
- 聚类与错配防护相关5项测试本地/远端均通过、退出0；
  远端最新全套**141项通过、退出0**（单测禁用CUDA，不冒充GPU质量验收）。
  用真实六份报告及两份历史基线完成整条收集链校验。冻结评测/训练源码和门槛均未改。
- **下一步不是继续加训**：先把r8的短TL内容/停顿风险留在修复清单，
  后续若获得可追溯的真正短对白文本或稳定可复现声学坏例，再冻结独立回归与单变量数据实验。
  没有这些新证据时暂停扩训，不凭“训练短录音比例低”直接提高剂量；
  不等待drama或强迫用户评母语质量，也不自动升级生产。

## 训练侧审计结果

入口 `scripts/training_coverage.py`，产物在大盘 `training_coverage_20260918/`。
只读取已有清单/录音和缓存模型，没有新增数据下载、切片、转码或自动改转写。

| r8目标语种 | 训练时长 | 音频时长中位数 | 3–8秒录音占训练时长 |
| --- | ---: | ---: | ---: |
| TH | 12.7872h | 6.17s | 54.82% |
| TL | 12.7847h | 11.74s | **9.40%** |
| VI | 12.7851h | 6.11s | 57.94% |
| ID | 12.7833h | 7.27s | 46.08% |
| MS | 12.7831h | 9.93s | **16.17%** |

- r8的32,307条记录均能通过 `source_id + origin_audio` 回到raw清单，文本逐字一致；
  候选池61,025条也全部一致。**这检查的是清单关联，不证明人声与文字逐字对齐。**
- 重新核对当前评测文本和源val，候选池没有新增需排除项；沿用既有身份/会话隔离规则。
- TL同源候选只有323条3–8秒录音，合计0.5668h；MS有663条、1.1663h。
  在12h联合预算、每语种17%的条件下，把某语种短录音时长份额设为25%需要0.51h，两者都够一次曝光。
- 但按r8约12.78h的单语种预算，即使把TL短录音重复到3次，上限也只有**13.30%**。
  因而不能把小预算试验直接放大成75h配方，更不能靠超额重复凑25%。
- 本机归档请求只有TL和TH两条示例；它们支持“实际调用包含短cue”，**不能估计五语种线上长度分布**。
  不拿不同语种的字符数、词数作统一语速或质量指标。

本次选择TL而不同时改TL/MS：TL短句时长份额最低，同源库存可支持方向试验。
25%是事先指定的试验剂量，不是最优比例，也不预设短句增配一定有效。

## 30条真实录音内容抽查：已完成

固定随机种子20260918；TL/MS各按3–5秒、5–8秒、8–30秒取5条非数字字符样本。
直接复用原录音；30条重新检查波形及文件SHA256，不按转写结果重新抽样。
使用已缓存large-v3、temperature=0、VAD关闭；TL自动检测，MS显式指定ms。
MS返回的语言字段因此**不是马来西亚口音/语种验证**。

| 分层 | 条数 | 平均CER | 最大CER |
| --- | ---: | ---: | ---: |
| TL 3–5s | 5 | 0.0451 | 0.0755 |
| TL 5–8s | 5 | 0.0377 | 0.0851 |
| TL 8–30s | 5 | 0.0357 | 0.0625 |
| MS 3–5s | 5 | 0.0146 | 0.0732 |
| MS 5–8s | 5 | 0.0217 | 0.1087 |
| MS 8–30s | 5 | 0.0074 | 0.0370 |

该抽样未出现整句偏题的转写信号，但存在词/拼写差异。
不将ASR当权威文字，不自动删录音或改训练标签；没有完成逐条母语/声学验收。
权重文件、抽样文件、原WAV哈希及逐段ASR结果保存在 `asr/plan.json`、`asr/results.json`。

## 已冻结的A/B

入口 `scripts/short_cue_ab.py`；工作目录 `/root/autodl-tmp/short_cue_ab_20260918/`。

| 项目 | A：control | B：tl_short |
| --- | ---: | ---: |
| 训练清单条数 | 5,164 | 5,282 |
| 清单音频时长 | 12.0257h | 12.0251h |
| TL条数 | 583 | 701 |
| TL时长 | 2.0429h | 2.0423h |
| TL 3–8秒时长份额 | 7.98% | 25.03% |
| 优化器更新步数 | **322** | **322** |
| 名义epoch | 0.9977 | 0.9754 |
| 目标录音最大曝光 | 1 | 1 |

- 两组从同一个官方基座新训，官方Accelerator初始化seed42；LoRA 64/64/dropout0.05，
  lr1e-4、batch2×累积8、warmup32、投影关闭、无ref、无控制前缀。
- 完整保留r8各源的时长权重及五目标语种17%/中文10%/英文5%；实际占比另存各组 `mix.json`。
  **非TL训练记录逐条相同**，不加入CV22、YODAS MS或其它新源。
- A按源内固定随机顺序取时长预算；B只把FLEURS TL拆为≤8秒和>8秒两档，按25%/75%取时长。
  全部来自已审核候选库存；不复制/修改音频。A短句7.98%不是强制复现r8的9.40%。
- **等更新步数，不冒称完全等训练暴露**：两组清单长度不同，shuffle后的实际取样顺序、
  被消费的音频小时数和语言份额不保证完全一致。两组均不足一个名义epoch，不跨epoch重复。
  这是固定更新预算下的抽样政策pilot，单训练seed不能证明纯时长因果效应。
- 6,613个训练/验证文件的音频头和SHA256已核对；按保守token上界确认不会被官方长度过滤静默删样本。
  基座、配置、清单、评测、训练脚本和依赖源码均记录哈希；复跑拒绝覆盖或隐式续训。
- 预检仅剩“ref不足30%”“可信控制标签不足25%”提示；两组均刻意保持r8这一输入形态，
  不为消除提示补造身份/情绪标签。

## 评测先冻结，不等训练后挑题

原v2的TL文本72–209字符，新150句留出中的TL文本81–267字符，均不能代表1–3秒cue的充分覆盖。
本次只用已经缓存的官方FLEURS test TSV，不下载音频或重新取源：

- 固定revision `70bb2e84b976b7e960aa89f1c648e09c59f894dd`，CC-BY-4.0。
- 排除已有raw、processed train/val、候选池范围及历史评测文本后，≤80原始字符的非数字TL文本
  只有**14句**，全部保留，实际57–80字符；≤40字符为0，≤60字符仅1。
  这是较短的朗读文本，**不是充分的极短对白测试集，尚不能保证合成时长≤8秒**。
- 每句分别配中/英/菲ref，轮换现有文件，得到42个输入条件；统计必须按
  `source_sentence_id` 聚合/重采样，不能把42条件或多seed当42条独立文本。
  已登记 `eval_cases/tl_short_holdout_20260918.jsonl`，供后续训练入口排除这些文本。
- 短句pilot比较base/r8/A/B，各42条件×5 seed，共840次生成；
  seed42/43/44/45/49、CFG1.8、20步、禁重试。
- 从原150句留出按五语种×三种ref语言固定随机选15句，只作跨语言明显回退的冒烟对照；
  A/B各15×5，共150次生成。base/r8复用已有同条件结果，不重跑。
- **计划正式pilot共990次生成**，不包括训练脚本自带的验证试听。
  小样本不能代替五语种完整验收；有收益信号后才安排完整留出、中英文回放及旧数字/坏例压力回归。

判断先看TL内容CER/疑似多读少读及非数字时长，再核对音色和明确声学异常。
沿用既有差值门槛，不因本轮数据少而放松；不以某条自动低分宣布语言自然度改善。
无收益则停止该剂量，不自动加epoch、扩大训练或替换r8。

## 执行状态与工程检查

- **最新状态：10:20:18完整990条已结束，配对分析及24次ASR诊断均已完成，结论见文首。**
  以下保留按时间发生的故障、恢复与调度记录，不代表任务还在排队。
- 数据覆盖审计、30条ASR抽查、A/B清单与配置预检均已完成。
- **两组训练均完成322步**，已交叉核对各自 `latest/training_state.json` 与完成标记，
  不重新训练、不续训r8。验证loss/total首末值：A **1.015644→0.877042**，
  B **1.018281→0.883851**（step0→321）；loss不能代替合成质量对照。
- 原串行控制器在首条base短句的声学评分阶段SIGSEGV，留下1条WAV，但没有完整评测报告。
  原 `status.json` 的 `failed`、日志和缓存原样保留；不是990条已完成或仍在训练。
  栈定位为 `audio_metrics → librosa.yin → localmin → numba gufunc`。
  复用该WAV检查：ASR成功，旧Numba缓存下声学评分复现崩溃；
  **只换缓存目录后，冷启动与第二个进程复用均成功，指标一致**。
  这定位到本轮旧缓存路径，不宣称查明了Numba底层缺陷或与本机退出134同因；
  不改依赖版本、不移除F0评分、不重跑训练。诊断仅复用已有WAV，没有额外TTS。
- **评测已按两路上限提交恢复**：`scripts/short_cue_eval.py` 为独立eval-only入口，
  输出在 `short_cue_ab_20260918/eval_recovery_20260918/`。
  原训练计划/脚本不改；启动前重新核对冻结源码、配置、6,613个训练/验证音频、
  base/r8及两组checkpoint，另记本次执行哈希。每个worker独享新的大盘运行缓存，
  启用故障栈；先等第一路产出完整单条指标，再启动第二路。
  三批分别为base/r8短句、A/B短句、A/B五语种冒烟，共**990条，不扩样本或更改seed/CFG/步数**。
  任一worker失败即停止同批另一进程及后续任务；逐条记录进度，低于1GiB可用空间停止，
  完整case/seed/元数据与参数校验后才写 `done`，不静默覆盖或自动重试。
  两并发沿用已验证预算，不声称是2/3/4路最优吞吐；并行的 `gen_sec` 不能当线上单请求延迟。
  当前进度看新目录 `status.json`、`*_progress.json`；`done`只表示生成/评分完成，**不代表验收通过**。
- **09:27:26恢复实测**：base短句38/210、r8短句1/210，合计39/990完成逐条评分，
  两路均已越过此前崩溃点；其余四任务按两路排队。采样显存19,390/49,140MiB，
  大盘仍约3.9GiB可用。不是990条完成或最终报告已落盘，实时数量以进度文件为准。
- **10:02按用户要求提前并行冒烟**：两组短句worker不重启，额外启动A的75条五语种冒烟；
  总并发上限暂为**3**，任一路结束释放名额后启动B的75条，不再等全部短句结束。
  新入口 `scripts/short_cue_overlap.py` 仅接管调度，复用原 `short_cue_eval.py worker`；
  冻结计划、评测代码、权重、case与seed均未修改，仍是原990条，不重复生成。
  接管记录为同目录 `overlap_schedule.json`，包括原状态、PID/启动时间与源码/计划哈希。
  旧控制器PID13835暂停，防止它随后重复启动冒烟；**暂停的是控制器，不是两个GPU worker**。
  新控制器核对原worker退出状态及全部报告后结束旧控制器；任一任务失败仍全体停止，
  通过PID启动时间防误杀复用PID。不要手动恢复旧控制器或重复启动原run。
  当前实时状态仍看同目录 `status.json` / `*_progress.json`，接管日志为 `overlap.log`。
  本次只确认提前启动，不把3路冒充吞吐最优或承诺相同比例提速。
  **10:05:29实测三路均持续产出**：短句A189/210、B188/210，冒烟A3/75，
  加已完成的base/r8共800/990；显存约28/48GiB。冒烟B尚未启动，等名额释放自动接上。
- 接管相关5项测试本地/远端均通过，覆盖总并发上限、不重启原任务、不重复冒烟、
  失败清理、PID复用保护；另用远端真实子进程核对Linux退出码读取。
  接管后远端全套**140项通过、退出0**（单测禁用CUDA），不是GPU质量验收。
- 本轮恢复相关5项测试本机/远端均通过、退出0；覆盖不完整/错配报告拒收、
  worker失败终止同批任务及不启动后续任务。远端完整**137项通过、退出0**，
  单测时设置 `CUDA_VISIBLE_DEVICES=""`，不冒充GPU质量验收。原冻结训练脚本未修改。
- 本地新增相关5项测试通过且退出0；远端当轮完整135项通过且退出0。
- 本机全套运行显示141项、后续143项断言通过，但在退出时遇到
  `libc++ recursive_mutex lock failed`，退出134，不计为整套成功；
  排除本轮新增文件的既有140项另跑退出0。未修改无关依赖或规避退出检查。
- 首次启动在模型加载前被空目录守卫拦住：共享preflight会预建输出目录。
  已补“预检留下空目录”的失败回归，再将守卫改为仅拒绝非空目录/非目录路径。
  原失败状态、脚本和计划另存，确认两组目录均为空后才重新提交；不是已有权重续训。
- 代码、文档、配置和小型评测元数据同步/归档；不把训练音频或整份训练集下载到本机，
  实验期间未提交/push；随后2026-09-18按用户要求以`4d8e092`提交并push，
  同时补齐`docs/runs.md`的r10/r11记录。不部署、不改变生产模型。
