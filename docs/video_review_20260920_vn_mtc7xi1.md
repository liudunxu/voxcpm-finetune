# 2026-09-20：VN新成片 mtc7xi1 对比

## 结论

**优先修源时间轴、参考音和复核状态，不启动微调。**
第2句不再硬裁，稀疏长窗保护已在新产物出现；但第13句源时间窗未改，
不能把“保护生效”写成“配音已对齐”。本轮没有人工听评，语言自然度仍未验证。

## 产物身份

- 用户视频：`vn-11110136-6v3u0-mtc7xi1rby0zc3.mp4`。
- SHA256：`89a4e2c67e86cb6b260679de83e8cdacf46a0ce882f585df671c78b0d28022d2`。
- 时长78.111995秒，源语配置en、目标vi，14条全部标TTS，没有显式`keep_original`。
  这不证明每条实际混音可懂、也不证明没有背景原声残留。
- meta创建时间`2026-09-20T08:59:45Z`（北京时间16:59:45），字幕附加时间
  `09:19:43Z`（17:19:43）；不是同一个阶段的结束时间。
- drama_id27；与审核27/90旧成片的原文件名、14条源文/译文/时间窗/speaker完全一致。
  65.8秒缩放到360×640的解码画面MD5也一致，支持同集素材对比，
  不代表已核验完整源文件SHA、新任务ID或线上锁定脚本版本。
- 客户端模型配置仍写`openbmb/VoxCPM2`，不能据此认定用了基座或覆盖用户已切r8的说明；
  最终生成参数可查，渲染时实际权重SHA仍未取得。

## 重点片段

| cue / 时间 | 新产物证据 | 判断 |
|---|---|---|
| 2 / 6.58–9.60s | raw 5.965→3.805s，最终3.020045s，tempo 1.238x；hard_trim由true变false，refit由true变false | 旧硬裁问题在这一条的meta中消失；最终ASR pass不等于母语发音验收 |
| 4 / 14.68–16.08s | 1.25x后仍`complete=false`、`requires_translation_refit=true` | 缩译/时长闭环未完成，不能只看内容ASR pass |
| 5–6 / 16.72–22.44s | 两条仍有severe speaker_mismatch；cue6参考仍带noisy_reference/low_snr | 优先参考与身份，不直接推定模型克隆能力不足 |
| 7 / 24.34–25.74s | 原take 6.88s，word-alignment salvage成2.08s；最终文本ASR pass，原output_vocalization severe保留 | 文本救回不是声学质量通过；6.88s的原QC/F0不能冒充2.08s成品指标 |
| 8 / 30.00–31.22s | 源文仍为`Thank you.`，参考low rms；局部源ASR没有支持这句的转写 | 源文本待核验，不能因TTS能读出`Cảm ơn`就通过源识别 |
| 12 / 57.00–58.28s | `speaker_assignment_risk=true`但`needs_review_reason=""`；最终QC Qwen→中文“看”，Whisper指定vi→`CẢM ƠN!`后放行 | ASR分歧不是已证实串语种；文本通过也不能解除身份风险 |
| 13 / 65.16–73.32s | raw2.745s→segment1.745011s，tempo1；有`unverified_sparse_source_window`，源窗仍8.16s | 保护已生效，但源时间轴未修；不是把两词读满8秒 |
| 14 / 74.44–77.64s | 1.25x后segment3.728005s，仍refit；摆放起点74.535s，文件尾78.111995s | segment名义尾部超视频约0.151s；不能单凭整段ASR pass排除最终混流尾部风险，也未证实裁掉了音节 |

新片severe仍是cue5/6/7三条，待refit由三条减为两条（4/14）。
非空复核原因由9条减为7条，**不能把这一数量变化当质量提升指标**，cue12已有漏标。
cue13实际仍用`episode_current`，明确抑制身份不确定时借回参考；
锚点列表`[14]`不能用来断言重新借用了cue14。

## 第13句：时间轴与可懂度分开

旧审核源片64–74.4秒的无提示Qwen ASR已定位人名在72.96–73.28秒，
转写为同音异字`之夏`。这份证据原样复用，没有反复识别挑结果。
新成片meta仍把生成语音安排在约65.20–66.76秒，计划起点早约7.76秒；
73.1秒画面可见人物张口及烧录字幕`Chi Hạ`，65.8秒仍是手臂特写。
画面不能单独证明说话人，烧录字幕也不是精确词级对齐真值。

本轮对新成片同一64–74.4秒混音做Qwen与Whisper两次无语言/期望文本提示的ASR，
均无转写；Qwen仅关闭VAD再查一次仍为空。**不能据此称音轨静音、删掉cue、
已经发音正确或已经修好**，也无法用这三次结果确认实际可听起点。
因此保留“meta安排过早”和“成片可懂度/是否混入正确take待核对”两个不同问题。
下一步优先取已有raw/segment/voice-only，区分生成、摆放和混音，不再扩ASR网格。

## 源识别与复核状态

- 源片29–33秒无提示Qwen ASR为空；56–60秒只识别出前句中文尾部
  `医生，介入吧。`，且对齐不完整，没有识别到`Thank you.`。
  这是cue8/12源文本可疑的线索，不是足以自动删除台词的证据。
  不因为这些中文片段就修改整片en配置，先查原识别结果和脚本编辑来源。
- 当前DIS源码可复现：`clear_resolved_clone_review`清除候选质量原因时，
  若`speaker_assignment_risk`仍为true，`needs_review_reason`可以变空；
  文本复核由uncertain转pass的清理也存在相同类别问题。
  cue12与这一失效模式一致，但meta不能还原所有中间状态，不能宣称已完整复现线上调用链。
  后续修复应保留尚未解决的源身份风险，不新增ASR/TTS请求，也不把改善音色当成角色归属已确认。

## 证据与后续顺序

证据目录：`checkpoints/video_review_20260920_mtc7xi1/`。
原视频、解码meta、逐cue对比、3个诊断WAV、2张截图及5次新ASR响应均保留；
`verification.json`记录16份本次/复用文件指纹及可执行断言结果。
完整meta、视频和音频不入Git；没有新TTS、训练、部署或线上脚本修改。

1. 新脚本版本核验cue13实际发声边界；取既有take/ref，校正时间与参考来源后仅局部重放。
2. 核验cue8/12源文及角色；修复候选/文本风险解除时把源身份复核状态一并清空的缺口。
3. 闭环cue4/14缩译与最终视频尾部检查，不用进一步加速或硬裁掩盖超窗。
4. 正确文本、时间、身份、ref及有效采样参数固定后仍稳定复现裸TTS问题，再考虑base/r8对照。

## 后记：调用方工程修复（同日，未部署）

DIS共享清理/最终收尾会恢复仍存的源身份或稀疏窗复核；
缓存独立检查`speaker_assignment_risk`，任务层也显示旧空reason的风险。
既有缩译后使用最终cues附字幕一次，并记录拒绝/失败/残留，不宣称requires_refit已解决。
新回归旧代码21失败，修后扩展531通过；没有新TTS/ASR/部署或训练。
cue13旧锁定窗仍需下一脚本版本核验，不把工程漏标修复当作角色归属或音色已修好。
详情见DIS `docs/reviews/2026-09-20-selected-take-and-refit.md`。
