# 2026-09-20：VN审核27/90与两词长窗口

## 结论

先优化源时间轴和合成后处理，**保持r8，不启动微调**。
第13条`Tri Hạ.`只有两个越南语音节/空格词；8.16秒是锁定脚本窗，
裸TTS1.05秒、最终segment1.072494秒，并非模型把它读成十秒。
本轮没有改线上脚本、部署、重渲或完成语言/音色听评。

## 证据

- drama27，第4集（episode_id90），revision67/version8/LOCKED，generation78/QC_READY。
  已取得用户导出的审核JSON和对应源片/成片，不再等待浏览器登录。
- 第13条源文`知夏。`，时间65.16–73.32秒。原TTS1.05秒经0.9x有界减速成1.072494秒，
  没有补静音；源能量区间跨度8.14秒，但confidence仅coarse。
- 一次无期望文本/无指定语言的局部源ASR（64–74.4秒）返回zh、`之夏。`，
  带词时间72.96–73.28秒。meta配音语音约65.204秒起，
  因此优先怀疑源时间窗过宽、配音提前约7.8秒，而非模型发音时长问题。
- 这是单次ASR诊断，同音字与原文并不严格一致，不自动写回version8；
  源语配置en与这段zh识别需要核对，不凭源文或烧录越南字幕改整片语言。
- 第2条另有明确硬裁和待缩译；4/5/6/7/13有身份/弱参考风险。
  第13条sync_note已有“身份不确定，抑制弱参考借回”，不能仅看锚点列表[14]就说仍借错人。
- 文件及SHA冻结在`checkpoints/review_27_90_20260920/`，完整meta/音视频不提交。

## 已做的系统性保护

DIS复用现有稀疏源文规则：源文1–2个既有alignment token、窗口>3秒；
不是新增“所有少于5词只能读几秒”的硬门。

1. 识别：文本匹配但ASR词跨度异常长，不标已校准；旧adjusted状态也不能直接跳过复核。
   可走已有SRT局部对齐，可信新词边界才采用，失败仍保留原时间。
2. 合成后处理和单句试听：未验证的短文长窗不再强行减速填空白，已有高置信节奏拟合及超窗保护不变。
3. 诊断：保留粗边界的原始误差数值，但标源时间待核验，不声称已证明“提前结束7秒”；
   既有身份/内容QC风险不覆盖。

正常路径不新增模型调用、不加采样步数/重试，不全片双ASR。
异常原本误判adjusted的SRT条目可能增加一次已有局部对齐；不是对全部自动ASR或已锁定项目自动重写。
CPU回放本case tempo由0.9改1.0，不代表旧成片已修复。

## 验证与下一步

- 新断言旧源码10失败/11通过，修后21项通过；扩展345项中342通过。
  另3项旧fixture缺segments失败在旧源码快照复现，未改无关测试。
- 补丁已应用实际DIS仓库，保留之前最佳TTS策略改动；实际目录159项定向回归通过。
  功能提交`79b7af4`保留远端6条提交后合并为`3dc9477`，合并后343项相关回归通过，
  已push至既有分支；未部署，不修改线上已锁定版本。
- 详细实现、产物指纹和分层诊断见
  `../../dubbing_intelligence_service/docs/reviews/2026-09-20-vn-sparse-source-timing.md`。
- 下一步部署核验后在新脚本版本修第13条边界，局部回放，检查实际参考和语音起点；
  利用下一cue前的安全余量容纳自然TTS，不硬压进约0.32秒源词窗。
  同时核对第2条既有缩译闭环，保留旧脚本/meta。
- 正确输入与后处理固定后，若裸TTS仍稳定失败，才冻结base/r8局部对照决定是否加训。
