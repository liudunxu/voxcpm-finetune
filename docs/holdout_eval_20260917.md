# 新文本 base/r8 对照与诊断

日期：2026-09-17。生产保持 r8 / strength1 / CFG1.8 / 20步，不启动 r11。

## 执行状态与范围

- 150条文本，TH/TL/VI/ID/MS各30条；seed42/43/44/45/49，base/r8各750条。
- **1500条生成/ASR、1500条两两音色分析、case配对统计全部完成**。
  控制器于2026-09-17 20:59:30写入 `done`，状态为 `complete`；原始报告与分析结果已备份。
  另行追加的24次ASR复核也已完成。没有重跑TTS，没有变更生产。
- 官方FLEURS test固定版本文本，排除审计范围内现有训练/验证/raw/候选池/历史评测文本。
  不证明基座预训练未见、语义独立或新说话人泛化；非数字朗读不能替代数字/括注、
  生产短cue和中英文回放验证。13个ref文件不等于13个已验证独立说话人。
- 推理固定CFG1.8、20步、禁重试；r8 strength1。ASR large-v3，仅TL自动判断语言。
  文本哈希 `2702e9eb5e876248eb21e5b18a3be21eb8c892d6640188dcf99c5977902abc82`。

## 五语种内容诊断

每语种30个case，先在case内平均5 seed，再按case等权汇总。
区间为5000次case配对bootstrap的95%百分位区间，差值均为r8−base。

| 语种 | CER base→r8 | ΔCER [95%区间] | 疑似漏尾率 base→r8 | 当前内容判断 |
| --- | --- | --- | --- | --- |
| TH | .0509→.0558 | +.00487 [−.00511, +.01694] | .1000→.1067 | 不明确；没有证明收益，也没有证明回退 |
| TL | .0373→.0138 | −.02350 [−.06375, +.00031] | .0400→0 | 均值改善，但区间跨零 |
| VI | .0402→.0259 | −.01429 [−.04857, +.01291] | .0067→.0067 | 均值改善，但区间跨零 |
| ID | .0359→.0143 | −.02155 [−.04649, −.00177] | .0800→.0333 | 有内容改善信号 |
| MS | .0540→.0140 | −.04004 [−.10187, −.00138] | .0533→0 | 有内容改善信号 |

WER为TL .0784→.0577、VI .0580→.0422、ID .0554→.0306、MS .1053→.0481；
TH不按空格计算WER，VI的单位也不等同于其他语言的词，不能横向排名。
区间只针对固定模型/ref/seed下的case变动，没有修正多重比较、共享ref依赖，
没有训练seed复现；不把ID/MS的信号升级为完整语言验收通过。
TH仅命中现有CER噪声提示，没有语种越过ΔCER>0.05提示线；未触发不等于已证明等效。

## 音色与时长：与内容分开

两两音色分析已完成。下表为WavLM组均值，每语种跨seed有30个case组，
跨cue有65个同ref/seed组；按组等权，不把两两组合当独立样本。

| 语种 | 跨seed base→r8 | 跨cue base→r8 | 稳定性诊断 |
| --- | --- | --- | --- |
| TH | .978408→.980136 | .960635→.962149 | 均值略升，局部跨cue下降 |
| TL | .982592→.982730 | .974146→.974235 | 均值近乎持平，局部跨seed下降 |
| VI | .976364→.979702 | .964031→.965997 | 均值略升，局部跨cue下降 |
| ID | .977648→.979946 | .970612→.972181 | 均值略升，局部跨seed下降 |
| MS | .973794→.979196 | .964192→.967704 | 均值略升，不代表没有局部风险 |

五语种两项均值都上升，但差幅小，未计算音色差值的置信区间，也没有人工确认，
**不能写成所有case更稳定或完整验收通过**。配对后最明显的局部下降：

- VI `fleurs_test_vi_1856 / fleurs_test_vi_1874`，ref_tl_04、seed43：
  跨cue余弦 .949904→.886011（Δ−.063893）。两条r8 CER分别.0183/0，内容低误差不等于音色一致。
- TH `fleurs_test_th_1739 / fleurs_test_th_1707`，ref_tl_03、seed42：
  .977742→.931732（Δ−.046010）；同组seed44也下降.042613。
- TL `fleurs_test_tl_1944` 的跨seed组均值 .967060→.943400；
  VI `fleurs_test_vi_1871` 为 .959178→.935939；
  ID `fleurs_test_id_1878` 为 .982022→.966800。

这些是自动诊断，不贴娃娃音/音色跳变标签。与ref相似度是另一维度，不能替代上述两两结果：

| 语种 | 与ref相似度 base→r8 | 平均音频秒数 base→r8 | p90尾静音秒数 base→r8 |
| --- | --- | --- | --- |
| TH | .8995→.8963 | 8.5056→8.3189 | .16→.28 |
| TL | .9177→.9207 | 10.5003→10.9248 | .20→.28 |
| VI | .9054→.8996 | 8.5227→8.1525 | .20→.30 |
| ID | .9142→.9139 | 9.3355→9.5189 | .16→.30 |
| MS | .9232→.9208 | 8.1525→8.4384 | .18→.28 |

本集全为非数字，同口径的时长膨胀/有声占比门禁均未触发。音频变长本身不能判坏：
例如 `fleurs_test_tl_1680` 需要结合内容恢复情况看，不能仅凭时长挑失败。
TH尾静音增加120ms、ID增加140ms，是真正超过既定100ms增量提示线的项目。
所有语种p90尾静音仍低于0.5s绝对上限；这不是全部单条音频的上限。

**已修工程边界问题**：冻结版本 `_duration_gates` 使用浮点减法严格比较，
`.28−.18` 被表示为略大于`.10`，导致overall/MS恰好100ms也被提示，
而`.30−.20`没有提示。不能把这两个边界误报算作实际超过阈值。
控制器完成后才修正共享指标增量比较，统一保留12位小数以去除远小于报告精度的浮点尾差；
同类CER、时长比例、有声占比边界一并覆盖，既定阈值与严格/包含边界语义不改。
新增测试检查恰好边界、真正越界、overall/by_lang及NaN内容分。
原始 `comparison.json` 保留，修正结果另存 `gate_fix/comparison.json`，前后源码也留档。
修正后仅TH/ID保留尾静音提示，overall/MS两个100ms边界误报消失；
CER噪声提示不变，没有新增或消失的CER红线，不手改历史报告、不重跑生成。

## 已生成的诊断样本

独立脚本 `scripts/holdout_diagnostics.py` 只引用已有音频：

- 15个诊断case：case均值CER正向差最大的10条，加与ref相似度下降最大的5条，去重。
  每case选使首个入选指标的base/r8配对差最差的seed；这是结果富集，不代表随机坏例率。
- 15个随机对照：复用现有抽样函数，五语种×中/英/菲ref语言各1条，RNG20260917、seed42。
  抽样发生在结果存在之后，但算法只用元数据；没有按结果排除诊断case。
  两组重合 `fleurs_test_vi_1695`、`fleurs_test_vi_1848`，如实保留，不假装相互独立。
- 两组分别生成base/r8盲听报告，共30对；检查case/seed、输入哈希和音频存在，
  重算小报告聚合，原始报告与人工标注不改。完整5 seed证据仍在原始报告。
- **未做人工声学评分**，没有自动贴金属音/娃娃音标签；可选只标明确异常。
  用这些样本选修法后降级为开发回归，不再把它们当最终未用验收集。

优先复核的三个内容诊断：

| case | base→r8的5-seed平均CER | 当前证据，尚非人工确认 |
| --- | --- | --- |
| `fleurs_test_vi_1695` | .03834→.23288 | r8/42 CER .8973，12s音频的ASR出现重复段落；先排除转写循环，不直接认定模型重复朗读 |
| `fleurs_test_th_1990` | .03572→.15804 | r8/42 CER .5536，ASR缺后半文本；另4 seed低得多，先区分ASR漏识别与真实少读 |
| `fleurs_test_th_1706` | .09384→.16206 | 专名转写差异与疑似少读混杂；最差配对增量在seed44，不能只按r8绝对CER选seed |

另保留已有r8跨cue诊断 `id_nat_11/id_nat_24`、`tl_nat_09/tl_nat_22`，
具体ref/seed与音色距离见 `quality_20260917.md`。这不是新集独立泛化证据。
历史3条人工明确坏例来自旧**base**报告，r10的 `ms_nat_16` 风险来自**B**；
都不能改贴成r8已确认线上坏例。

## 小规模ASR复核

`scripts/holdout_diagnostics.py --recheck-asr` 复用前三个内容诊断case，
各取最差配对seed与seed49，base/r8共12条现成音频：
先重做原VAD开启转写，再只改变 `vad_filter=False`，共24次转写。
单独保存片段时间、转写、CER、输入音频哈希；不合成、不改模型、
不把另一套ASR条件的分数覆盖回原报告。`asr_recheck/results.json` 已为
`complete=true`，24/24完成。
这是验证评分器敏感性，不是母语评审；即使另一解码设置恢复文本，也不证明发音自然。

| 模型/case/seed | 原CER | 重复原VAD开启 | 关闭VAD |
| --- | ---: | ---: | ---: |
| base / th_1706 / 44 | .0664 | .1517 | .0616 |
| base / th_1706 / 49 | .0237 | .1754 | .2038 |
| r8 / th_1706 / 44 | .2180 | .0664 | .0664 |
| r8 / th_1990 / 42 | .5536 | .0179 | .0179 |
| r8 / vi_1695 / 42 | .8973 | .8973 | .8973 |

表内case省略共同前缀 `fleurs_test_`。**原设置复测本身就有差异**，有的变好、有的变差，
不能把全部差别归因VAD，也不能只用较低分宣布修复。TH高CER未能稳定复现，
不足以据此确认真实少读或决定补训练数据。VI同一条重复转写现象在两种VAD设置下均保留，
只能说明它不是本次关闭VAD即可消除的问题；还不能区分真实重复朗读与ASR循环。
保留原始全量分数，下一步先检查解码设置/回退及重复性，不把这24次非随机复核
替换进全量均值或bootstrap区间；原区间没有计入ASR复测方差。

## 产物与下一步

远端 `/root/autodl-tmp/holdout_eval_20260917/`：
`base_report.json`、`r8_report.json`、`comparison.json`、`voice_stability/`、
`diagnostic_sample/{plan,reports}.json`、`asr_recheck/results.json`。
完整评测源码快照不覆盖；诊断导出与ASR复核的追加脚本分别留版本快照。
本地仅备份报告/元数据/源码，不下载训练数据或源音频。
新增导出与ASR复核检查已通过；边界修正后本地全套133项通过，
远端本次执行相关28项并通过，不把局部测试冒称远端全套通过。

1. **保持r8，不扩大r10、不启动r11**。ID/MS有内容改善信号；其余内容结论不明确，
   音色组均值略升但仍有局部下降，不以均值掩盖问题，也不推翻已有生产短cue诊断。
2. 先在同一批已有音频上固定并验证ASR解码重复性，补查参数/回退与随机性记录；
   不先调TTS参数、删case或改训练文本来迎合不稳定评分。
3. 优先定位 `vi_1695/42` 的重复转写及上述VI/TH跨cue组，分开检查ASR、ref与生成裸输出；
   需要新生成时再冻结单变量小实验，不重跑整个网格，不要求母语评分才能推进。
4. 只有可复现问题、明确改进目标和已审核干预数据同时具备，才设计同预算单变量r11。
   当前没有据此指定新数据比例。自然度/口音/情绪仍未验证。
