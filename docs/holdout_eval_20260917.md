# 新文本 base/r8 对照与诊断

日期：2026-09-17。生产保持 r8 / strength1 / CFG1.8 / 20步，不启动 r11。

## 执行状态与范围

- 150条文本，TH/TL/VI/ID/MS各30条；seed42/43/44/45/49，base/r8各750条。
- **1500条生成/ASR、1500条两两音色分析、case配对统计全部完成**。
  控制器于2026-09-17 20:59:30写入 `done`，状态为 `complete`；原始报告与分析结果已备份。
  另行追加的24次VAD复核、36次ASR重复性复核及2次词级对齐也已完成。
  本次固定随机对照的180次ASR与局部音色的80次CPU嵌入也已完成，均复用既有音频。
  22:10:05追加完成120次句首/中段/句尾窗口嵌入，并导出20对完整cue声学试听报告。
  22:26:02另完成10对同模型双cue材料，40段原PCM校验及20条文件的6006读取检查通过；人工仍未评。
  没有重跑TTS，没有变更生产。
- 官方FLEURS test固定版本文本，排除审计范围内现有训练/验证/raw/候选池/历史评测文本。
  不证明基座预训练未见、语义独立或新说话人泛化；非数字朗读不能替代数字/括注、
  生产短cue和中英文回放验证。13个ref文件不等于13个已验证独立说话人。
- 推理固定CFG1.8、20步、禁重试；r8 strength1。ASR large-v3，仅TL自动判断语言。
  文本哈希 `2702e9eb5e876248eb21e5b18a3be21eb8c892d6640188dcf99c5977902abc82`。

## 五语种内容诊断

每语种30个case，先在case内平均5 seed，再按case等权汇总。
区间为5000次case配对bootstrap的95%百分位区间，差值均为r8−base。

| 语种 | CER base→r8 | ΔCER [95%区间] | 疑似漏尾率 base→r8 | 当前内容判断 |
| --- | --- | --- | --- | --- |
| TH | .0509→.0558 | +.00487 [−.00511, +.01694] | .1000→.1067 | 不明确；没有证明收益，也没有证明回退 |
| TL | .0373→.0138 | −.02350 [−.06375, +.00031] | .0400→0 | 均值改善，但区间跨零 |
| VI | .0402→.0259 | −.01429 [−.04857, +.01291] | .0067→.0067 | 均值改善，但区间跨零 |
| ID | .0359→.0143 | −.02155 [−.04649, −.00177] | .0800→.0333 | 有内容改善信号 |
| MS | .0540→.0140 | −.04004 [−.10187, −.00138] | .0533→0 | 有内容改善信号 |

WER为TL .0784→.0577、VI .0580→.0422、ID .0554→.0306、MS .1053→.0481；
TH不按空格计算WER，VI的单位也不等同于其他语言的词，不能横向排名。
区间只针对固定模型/ref/seed下的case变动，没有修正多重比较、共享ref依赖，
没有训练seed复现；不把ID/MS的信号升级为完整语言验收通过。
TH仅命中现有CER噪声提示，没有语种越过ΔCER>0.05提示线；未触发不等于已证明等效。

## 音色与时长：与内容分开

两两音色分析已完成。下表为WavLM组均值，每语种跨seed有30个case组，
跨cue有65个同ref/seed组；按组等权，不把两两组合当独立样本。

| 语种 | 跨seed base→r8 | 跨cue base→r8 | 稳定性诊断 |
| --- | --- | --- | --- |
| TH | .978408→.980136 | .960635→.962149 | 均值略升，局部跨cue下降 |
| TL | .982592→.982730 | .974146→.974235 | 均值近乎持平，局部跨seed下降 |
| VI | .976364→.979702 | .964031→.965997 | 均值略升，局部跨cue下降 |
| ID | .977648→.979946 | .970612→.972181 | 均值略升，局部跨seed下降 |
| MS | .973794→.979196 | .964192→.967704 | 均值略升，不代表没有局部风险 |

五语种两项均值都上升，但差幅小，未计算音色差值的置信区间，也没有人工确认，
**不能写成所有case更稳定或完整验收通过**。配对后最明显的局部下降：

- VI `fleurs_test_vi_1856 / fleurs_test_vi_1874`，ref_tl_04、seed43：
  跨cue余弦 .949904→.886011（Δ−.063893）。两条r8 CER分别.0183/0，内容低误差不等于音色一致。
- TH `fleurs_test_th_1739 / fleurs_test_th_1707`，ref_tl_03、seed42：
  .977742→.931732（Δ−.046010）；同组seed44也下降.042613。
- TL `fleurs_test_tl_1944` 的跨seed组均值 .967060→.943400；
  VI `fleurs_test_vi_1871` 为 .959178→.935939；
  ID `fleurs_test_id_1878` 为 .982022→.966800。

这些是自动诊断，不贴娃娃音/音色跳变标签。与ref相似度是另一维度，不能替代上述两两结果：

| 语种 | 与ref相似度 base→r8 | 平均音频秒数 base→r8 | p90尾静音秒数 base→r8 |
| --- | --- | --- | --- |
| TH | .8995→.8963 | 8.5056→8.3189 | .16→.28 |
| TL | .9177→.9207 | 10.5003→10.9248 | .20→.28 |
| VI | .9054→.8996 | 8.5227→8.1525 | .20→.30 |
| ID | .9142→.9139 | 9.3355→9.5189 | .16→.30 |
| MS | .9232→.9208 | 8.1525→8.4384 | .18→.28 |

本集全为非数字，同口径的时长膨胀/有声占比门禁均未触发。音频变长本身不能判坏：
例如 `fleurs_test_tl_1680` 需要结合内容恢复情况看，不能仅凭时长挑失败。
TH尾静音增加120ms、ID增加140ms，是真正超过既定100ms增量提示线的项目。
所有语种p90尾静音仍低于0.5s绝对上限；这不是全部单条音频的上限。

**已修工程边界问题**：冻结版本 `_duration_gates` 使用浮点减法严格比较，
`.28−.18` 被表示为略大于`.10`，导致overall/MS恰好100ms也被提示，
而`.30−.20`没有提示。不能把这两个边界误报算作实际超过阈值。
控制器完成后才修正共享指标增量比较，统一保留12位小数以去除远小于报告精度的浮点尾差；
同类CER、时长比例、有声占比边界一并覆盖，既定阈值与严格/包含边界语义不改。
新增测试检查恰好边界、真正越界、overall/by_lang及NaN内容分。
原始 `comparison.json` 保留，修正结果另存 `gate_fix/comparison.json`，前后源码也留档。
修正后仅TH/ID保留尾静音提示，overall/MS两个100ms边界误报消失；
CER噪声提示不变，没有新增或消失的CER红线，不手改历史报告、不重跑生成。

## 已生成的诊断样本

独立脚本 `scripts/holdout_diagnostics.py` 只引用已有音频：

- 15个诊断case：case均值CER正向差最大的10条，加与ref相似度下降最大的5条，去重。
  每case选使首个入选指标的base/r8配对差最差的seed；这是结果富集，不代表随机坏例率。
- 15个随机对照：复用现有抽样函数，五语种×中/英/菲ref语言各1条，RNG20260917、seed42。
  抽样发生在结果存在之后，但算法只用元数据；没有按结果排除诊断case。
  两组重合 `fleurs_test_vi_1695`、`fleurs_test_vi_1848`，如实保留，不假装相互独立。
- 两组分别生成base/r8盲听报告，共30对；检查case/seed、输入哈希和音频存在，
  重算小报告聚合，原始报告与人工标注不改。完整5 seed证据仍在原始报告。
- **未做人工声学评分**，没有自动贴金属音/娃娃音标签；可选只标明确异常。
  用这些样本选修法后降级为开发回归，不再把它们当最终未用验收集。

优先复核的三个内容诊断：

| case | base→r8的5-seed平均CER | 当前证据，尚非人工确认 |
| --- | --- | --- |
| `fleurs_test_vi_1695` | .03834→.23288 | r8/42 CER .8973，12s音频的ASR出现重复段落；先排除转写循环，不直接认定模型重复朗读 |
| `fleurs_test_th_1990` | .03572→.15804 | r8/42 CER .5536，ASR缺后半文本；另4 seed低得多，先区分ASR漏识别与真实少读 |
| `fleurs_test_th_1706` | .09384→.16206 | 专名转写差异与疑似少读混杂；最差配对增量在seed44，不能只按r8绝对CER选seed |

另保留已有r8跨cue诊断 `id_nat_11/id_nat_24`、`tl_nat_09/tl_nat_22`，
具体ref/seed与音色距离见 `quality_20260917.md`。这不是新集独立泛化证据。
历史3条人工明确坏例来自旧**base**报告，r10的 `ms_nat_16` 风险来自**B**；
都不能改贴成r8已确认线上坏例。

## 小规模ASR复核

`scripts/holdout_diagnostics.py --recheck-asr` 复用前三个内容诊断case，
各取最差配对seed与seed49，base/r8共12条现成音频：
先重做原VAD开启转写，再只改变 `vad_filter=False`，共24次转写。
单独保存片段时间、转写、CER、输入音频哈希；不合成、不改模型、
不把另一套ASR条件的分数覆盖回原报告。`asr_recheck/results.json` 已为
`complete=true`，24/24完成。
这是验证评分器敏感性，不是母语评审；即使另一解码设置恢复文本，也不证明发音自然。

| 模型/case/seed | 原CER | 重复原VAD开启 | 关闭VAD |
| --- | ---: | ---: | ---: |
| base / th_1706 / 44 | .0664 | .1517 | .0616 |
| base / th_1706 / 49 | .0237 | .1754 | .2038 |
| r8 / th_1706 / 44 | .2180 | .0664 | .0664 |
| r8 / th_1990 / 42 | .5536 | .0179 | .0179 |
| r8 / vi_1695 / 42 | .8973 | .8973 | .8973 |

表内case省略共同前缀 `fleurs_test_`。**原设置复测本身就有差异**，有的变好、有的变差，
不能把全部差别归因VAD，也不能只用较低分宣布修复。TH高CER未能稳定复现，
不足以据此确认真实少读或决定补训练数据。VI同一条重复转写现象在两种VAD设置下均保留，
只能说明它不是本次关闭VAD即可消除的问题；还不能区分真实重复朗读与ASR循环。
保留原始全量分数，不把这24次非随机复核替换进全量均值或bootstrap区间；
原区间没有计入ASR复测方差。追加的解码设置/回退及重复性检查见下节。

## ASR解码重复性：36/36完成

2026-09-17 21:23:14完成。`scripts/asr_repeatability.py` 只复用上述前三个诊断case
的最差配对seed，base/r8共6条WAV；不重新生成TTS，也不改训练数据。
每条默认解码/仅设 `temperature=0.0` 各3次，遍历顺序正/反/正，同一进程复用同一个模型。
VAD始终开启，TH/VI固定语言；这组音频按异常结果选出，不代表随机坏例率。

实测环境为faster-whisper 1.2.1、CTranslate2 4.8.2、torch 2.6.0+cu124、av 18.1.0，
设备cuda、实际计算类型 **int8_float16**。使用已缓存的large-v3固定快照
`edaa852ec7e145841d8ffdb056a99866b5f0a478`，不下载新模型。
`plan.json` 冻结原报告、WAV、模型文件及相关源码哈希；结果保存完整segment、VAD和解码选项。

| 模型/case/seed | 默认3次CER范围 | 零温度3次CER范围 |
| --- | ---: | ---: |
| base / vi_1695 / 42 | .034247 | .034247 |
| base / th_1990 / 42 | .053571 | .053571 |
| base / th_1706 / 44 | .066351–.189573 | .066351 |
| r8 / vi_1695 / 42 | .897260 | .897260 |
| r8 / th_1990 / 42 | .017857–.089286 | .017857 |
| r8 / th_1706 / 44 | .061611 | .061611 |

默认解码有 **2/6** 条原文及归一化转写波动；零温度 **6/6** 三次均一致。
远端安装源码核实：默认温度列表为0/.2/.4/.6/.8/1，正温度回退走采样。
当所有回退尝试均未满足条件时，库优先在未超压缩率门槛的候选内取最高平均logprob，
没有这类候选才比较全部尝试；但segment的
`temperature` 可能仍记最后一次尝试的温度，不能仅据该字段反推被采用候选的真实温度。
因此本轮记录实际选项与所有返回片段，不把TTS seed当作已控制ASR随机性的证明。

**结论只限评分重复性**：零温度是待验证的评分器候选，不是TTS修复。
未测试进程重启/换机，也未覆盖TL/ID/MS；稳定转写仍可能稳定地错。
通用 `voxft.eval._transcribe` 默认值未改，旧分数与bootstrap区间未替换。

## VI词级对齐：2/2完成

2026-09-17 21:34:37完成。同一脚本的 `--align-vi` 只读取上一实验
`temperature_zero/repeat0` 的 `vi_1695/42` base/r8音频，唯一新增参数为
`word_timestamps=True`；实际返回的解码选项也仅这一项不同。
再次核验模型和音频哈希，真实时长取WAV头；没有手动删除转写或覆盖既有结果。

| 模型 | WAV时长 | 零温度末段终点→词级对齐终点 | CER前→后 |
| --- | ---: | ---: | ---: |
| base | 14.72s | 14.50→14.48s | .034247→.034247 |
| r8 | 12.00s | 29.98→11.98s | .897260→.897260 |

r8原第二段标为11.14–29.98s，超出真实音频17.98s。
词级对齐后第二段为10.90–11.98s，但**重复文本完全保留**；
该段46个词级条目中42个的起止时间同为11.98s。base的49个词级条目均有非零时长。
这里的「词级条目」是库输出的对齐单位，不等于语言学意义的词数。

这是强烈的转写/对齐异常证据，不能直接把高CER归因为TTS整段真实复读；
但同一ASR的对齐也不是独立听音证据，仍不能宣布原音频发音、尾句或自然度已通过。
**时间戳不再越界，不等于内容已修复**。不按零时长条目机械删字降分，
不凭这两条结果启用新的全局过滤/解码规则，也不为迎合这个分数补训练数据。

## 固定随机ASR：180/180完成

2026-09-17 21:45:23完成。`scripts/asr_repeatability.py --random-controls`
直接使用已冻结的15条 `random_control`（五语种×zh/en/tl ref语言各1条，seed42），
base/r8共30条WAV。默认/零温度各3次，保持正/反/正遍历，不重新抽样、不按分数排除任何case。
与诊断组重合的 `vi_1695` / `vi_1848` 保留，不能把两组当完全独立样本。
仍使用原large-v3模型、VAD及语言规则；TL自动检测，其余固定语言。
新增记录真实WAV时长、片段终点、越界秒数、实际识别语言；没有把这些诊断字段变成新门禁。

| 语种 | WAV数（base+r8） | 默认：3次转写稳定 | 零温度：3次转写稳定 | 两设置转写是否一致 |
| --- | ---: | ---: | ---: | --- |
| TH | 6 | 6/6 | 6/6 | 是 |
| TL | 6 | 6/6 | 6/6 | 是 |
| VI | 6 | 6/6 | 6/6 | 是 |
| ID | 6 | 6/6 | 6/6 | 是 |
| MS | 6 | 6/6 | 6/6 | 是 |

稳定指原文和归一化转写均一致，CER极差均为0；30条也都与历史原报告的归一化转写一致。
TL的36次识别均返回tl。**默认组所有片段此次均报告temperature=0**，未走正温度回退；
因此这个结果不是「取消回退更好」的证据，更不能否定此前困难TH样本的默认解码波动。
本轮三次重复仍在同一个加载后的模型进程内；未专门做多次重启/换机或独立ASR准确性验证。

时间戳异常也没有被零温度消除：两种设置下均有3条WAV越界，
分别为base `th_1742` 0.04s、r8 `th_1742` 0.16s、r8 `vi_1695` 17.98s。
每条均重复3次，即各设置9/90次；不把小幅时间戳偏差和长段重复异常混为一类，
也不把「终点大于时长」直接改成TTS失败或自动删字规则。

**当前处置**：不改通用ASR默认，不重算全量，不按较低CER挑设置。
默认解码在随机样本稳定、在部分困难样本不稳定；后者按个案保留完整转写与时间证据即可，
停止继续扩大重复性实验，把工作转回音色问题。不能据此宣布五语种语言质量通过。

## 局部音色裁边：80/80完成

2026-09-17 21:48:35完成。`scripts/holdout_diagnostics.py --localize-voice`
固定此前已指出的TH `1739/1707`（ref_tl_03）与VI `1856/1874`（ref_tl_04），
各取全部seed42/43/44/45/49，base/r8共40条WAV。
两种输入为原样与仅按已有 `edge_silence` 测量在内存裁边，合计80次WavLM嵌入。
内部停顿、ref、音高、响度均未改变，不写新WAV、不合成TTS、不改生产裁切规则。
模型只从本地缓存加载，实际CPU/112线程，未设置CPU亲和性限制；模型处于eval模式。

原样20个跨cue组（2语种×5 seed×2模型）与既有报告的6位小数数值全部相同，
最大复现差为0，低于预先设定的1e-5容差。才进一步解释裁边敏感性：

| seed | TH Δ余弦：原样 | TH Δ余弦：裁边 | VI Δ余弦：原样 | VI Δ余弦：裁边 |
| --- | ---: | ---: | ---: | ---: |
| 42 | −.046010 | −.044534 | +.017058 | +.014045 |
| 43 | −.025465 | −.036863 | −.063893 | −.074993 |
| 44 | −.042613 | −.043472 | −.002331 | −.005841 |
| 45 | +.030979 | +.034080 | +.044252 | +.035555 |
| 49 | −.035278 | −.034262 | +.003974 | −.001859 |

Δ均为r8−base，同ref、同seed的两条不同文本比较；这些是诊断差值，不是新的验收阈值。
TH五seed组均值：原样 .949692→.926014，裁边 .947471→.922461；
VI则为原样 .950673→.950485，裁边 .953297→.946678。
**首尾静音不能充分解释局部下降**：TH在裁边前后均4/5 seed下降；VI43下降仍在且更大。
这不证明裁边能安全上线，也不等于真实听感已确认；只排查了首尾静音对该嵌入指标的影响，
没有排除内部停顿、文本/韵律敏感性或生产后处理差异。

复用本次保存的嵌入进一步定位（不再跑模型）：
- TH两句各自的原样跨seed均值分别为 .968047→.974905、.976374→.975813，
  但同seed跨cue均值下降。应优先查不同文本之间的声学差异，不能把问题简化成「换seed不稳定」。
- VI `1874/43` 与同句其余4个r8 seed的平均余弦为 .964964（裁边 .962828），
  在该句5个seed中最低；另一句 `1856/43` 为 .983184（裁边 .981159）。
  因而先定位 `1874/43`，不把整对或整个VI语种都标为已确认异常。

两组都是事后选出的诊断case，5个seed共享同一对文本/ref，不能当5份独立语言验收样本。
嵌入差异可能包含文本内容而非人感知的音色；金属音、娃娃音、语言自然度等仍未验证。

## 句首/中段/句尾定位：120/120完成

2026-09-17 22:10:05完成。`scripts/holdout_diagnostics.py --voice-windows`
复用上述40条WAV，在能量法测得的首尾边界内，分别取句首、中段、句尾各3秒，
合计120次CPU/112线程WavLM嵌入；复用已保存的40个裁边整句嵌入，不重跑前轮80次。
窗口可能重叠、保留内部停顿，**不是词/音素对齐，也不是连续无静音的3秒语音**。
所有窗口的能量法有声帧占比为.5839–.9732；不同文本/seed的同位置不保证对应同一内容。
没有新TTS/ASR、没有写裁片WAV或改变ref、后处理、生产设置。

执行前校验旧任务完成状态、plan哈希、输入报告/音频、共享源码、模型哈希、库版本和样本顺序，
拒绝旧嵌入与新音频混用；结束再次核验冻结输入与源码。模型未下载，原报告/音频未改。
结果保存每个窗口的起止、与自身裁边整句的余弦、有声占比、带内能量占比及两两统计，
嵌入另存npz，后续计算不必再推理。

同ref/seed的两句跨cue余弦差（r8−base）：

| seed | TH句首 | TH中段 | TH句尾 | VI句首 | VI中段 | VI句尾 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| 42 | +.007951 | −.044739 | −.076243 | +.037633 | +.044951 | +.046230 |
| 43 | +.137883 | −.008167 | −.144564 | −.030848 | −.067725 | −.098840 |
| 44 | −.090456 | −.138225 | −.012570 | −.019108 | −.013312 | −.034431 |
| 45 | +.026891 | −.005656 | +.081093 | −.073455 | +.104668 | −.000147 |
| 49 | −.097008 | +.046881 | −.051983 | −.082381 | −.065461 | +.016895 |

- **TH不是整句每处一致下降**：句首仅2/5 seed下降，中段/句尾各4/5下降；
  五seed组均值句首 .873607→.870660、中段 .939641→.909660、句尾 .862044→.821191。
  中后段是声学复核的优先位置，不据此宣布尾部音色漂移或制定裁尾规则。
- **VI43跨cue差异在三处都有，不只句尾**：表内Δ分别−.030848/−.067725/−.098840。
  但不能据此只归责 `1874/43`：该句与自身另4个r8 seed的同位置平均余弦，
  句首/中段/句尾为 .937326/.951760/.936312；句首、中段都不是5个seed中最低，
  句尾与seed45的 .936433也很接近。另一句 `1856/43` 的尾部相应值为 .919569。
  这些来自已存嵌入的点积，没有新增模型调用；没有定位到足以确认单段跳变的独立证据。
- **不继续扩大滑窗/参数扫描**：短窗指标与整句不能共用阈值；同一WavLM拆窗不是独立验证，
  更低余弦不能自动翻译成娃娃音、金属音或身份变化。先核对可听声学异常和发生环节，
  不把此次结果用来自动调训练比例、加epoch或启动r11。

20对**原始完整cue**试听报告已写入6006工作台可选报告列表：

- A：`holdout_local_voice_base_9c9793812e3a4294be42399dc87bd2b4.json`
- B：`holdout_local_voice_r8_0ed42d12877e4d4da2d91a34b9c792d4.json`

四条文本×五seed，按语种/seed/case排列；复用原WAV与逐条指标，仅重算子集汇总，
原人工字段保留，**没有人工声学评分，也没有母语评分**。
用户可选只标明确异常及时间点，不确定留空，不将其设为后续离线实验前置。
**现有UI逐case随机甲乙，相邻条目的「甲」不一定来自同模型**；
这批仅支持逐句配对，不冒称已提供组内模型固定的双cue连贯盲听。
若要判断跨cue变化，应使用下节另行生成的组内映射固定双cue材料，不能直接横比本节相邻条目的甲。

## 同模型双cue材料：10对完成，导出时人工未评

2026-09-17 22:26:02完成。`scripts/holdout_diagnostics.py --dual-cue-review`
仅复用此前固定的40条WAV，不新增TTS、ASR或WavLM：

- TH按 `1739 → 1707`，VI按 `1856 → 1874`；各取seed42/43/44/45/49。
  每个模型分别拼接同语种、同ref、同seed的两句，共10对/20条音频。
  这是「甲内两句同模型、乙内两句同模型」，**不是跨条目一直甲=某模型**。
- 只在两个完整原WAV之间加0.5秒数字静音；原头尾静音、响度、音高与全部PCM样本保留，
  不裁切、不淡入淡出、不重采样。保持48kHz/单声道/PCM_16，源格式不符直接拒绝。
  人为拼接点若出现疑似爆音或异常停顿，必须回原单句WAV核对，不能当作新发现的模型缺陷。
- 导出前冻结输入与执行源码哈希，导出后复查；40段原PCM逐样本一致，中间24000个样本全为零。
  20条音频共345.36秒（约5分45秒，不含操作时间），文件约33.2MB，全在远端大盘。
  新文件使用不含模型名的随机文件名，单句来源和拼接时间区间仅记录在报告的 `components`。
- 新的评分单位是双cue组，人工字段从未评开始，不把旧单句的标签自动汇总或传播。
  原报告、WAV、单句人工标注不改；两份新报告均标 `review_only=true`，不复制CER/WER/音色分数。
  共享 `_report_metrics` 明确拒绝这类拼接条目，不能靠 `--summarize` 把它们混进自动验收。

6006「盲听评估」切换Tab可刷新列表，选择：

- A：`holdout_dual_cue_base_1013d418b0ce45e381c6b41ff118b6ec.json`
- B：`holdout_dual_cue_r8_93f51759fb01472787eaa20b34be1b25.json`

每条分别听甲内两句与乙内两句，只标明确声学异常、类型与时间点；不确定留空，
不要求填写母语自然度/可懂度。标注写入这两份**双句报告**；后续定位到单句时，
使用 `components` 的原case/seed及起止样本换算时间，不能把整组标签自动贴给其中每句。
固定句序没有另做顺序效应对照；每语种实际上只有两句文本/一个ref，五seed不是五份独立语言材料，
结果富集的10对也不能估计线上坏例率。

工程验证已完成：

- 本地全套139项、远端相关32项测试通过，覆盖拼接样本/源文件不变、拒绝已变更输入/错误采样率、
  组内模型映射、现有UI载入函数、未评写回及自动汇总拒绝。
- 20条WAV经运行中的6006文件接口读取，均HTTP200，响应内容SHA256与导出文件一致。
  WAV放在当前UI允许的 `voxft_ckpt/eval/dual_cue_audio_*`，不放仅存元数据的工作目录。
  这是文件读取与函数级验证，**没有声称完成浏览器实机点击/播放或人工听评**，没有重启UI。
- 没有新增已确认的娃娃音/金属音/音色跳变结论；此轮完成的是复核材料与工程校验，
  不是模型修复或语言质量验收。生产r8/strength1/CFG1.8/20步不变，不启动r11。

## 本机离线双句试听：绕开6006音频超时

2026-09-17，用户浏览器的两条WAV请求报 `ERR_CONNECTION_TIMED_OUT` / `Failed to fetch`。
此前远端HTTP200校验不代表用户浏览器可达；本次不把超时判为坏音频，也不改Gradio或重启服务。
按用户要求通过SSH下载20条既有双cue WAV，共33,155,440字节；不重新合成、不转码、不调响度。
全部文件SHA256与冻结 `dual_cue/results.json` 一致，48kHz/单声道/PCM_16完整读取通过。

本机入口：
`checkpoints/holdout_eval_20260917/dual_cue/offline/index.html`。
同目录 `audio/` 保存音频，双击HTML即可使用；无外部脚本、字体、接口或JS fetch，不需要6006/SSH持续在线。
不要仅移动HTML而丢掉相邻音频目录。代号 `D01-A`–`D10-B`，每条内部仍是原来的同模型双cue；
组间随机映射不变，页面与音频路径不暴露模型名。第二句起始时间标在各播放器旁。

- 试听包：`dual-4883215cdc38`；映射在本机 `dual_cue/offline_key.json`，不嵌入页面。
  后续用户按代号反馈时必须用此映射还原，不沿用6006会话的甲乙顺序。
- 初始全部未评，不继承旧评分；只提供无明显异常/有明确异常/不确定与时间点备注。
  用户可直接回复代号，也可点击「生成并下载已填反馈」。未填项不导出为无异常或平局，
  不要求自然度/可懂度评分；填写不自动保存，关闭前需导出。没有自动写回原评测报告。
- `holdout_diagnostics.py --offline-review --work <本机holdout工作目录>` 复用既有配对守卫，
  校验冻结报告/音频后导出；已有页面或映射即拒绝覆盖，防止代号重排误接后续反馈。
- 本地全套140项通过（本次相关5项）；覆盖音频被改拒绝、相对路径可达、模型映射、
  未评初始化和禁止覆盖。另执行内联JS检查：未填不下载、代号/备注导出正确、播放时暂停其他音频。
  这是本地文件/逻辑验证，**不是已在用户浏览器完成播放或人工听评**。

页面SHA256：`69c37e494d30b2387f8049cc50f63d73b8726206a1da1cfcf6116ecf14c1f47d`。
映射SHA256：`fb2c47ae04ba0f23ccd022fcb3c2ea9a83bf7d4b20c12ba569bbacb96d480d97`。
本轮仅把已有试听材料带到本机；没有新增已确认声学异常，生产和训练计划不变。

## 离线声学反馈：1条明确模糊，3条不确定

2026-09-17，用户返回试听包 `dual-4883215cdc38` 的全部20个代号反馈。
先核对原映射SHA256、原报告与音频，再按固定映射导入；未重新随机甲乙。
原文保存在 `dual_cue/offline_feedback.txt`，带标注副本与汇总在 `dual_cue/feedback_review/`。
**没有覆盖冻结双cue报告、原单句报告、PCM、网页或代号映射**；后续分析需读取本次带标注副本。

| 语种 | 模型 | 无明显异常 | 明确异常 | 不确定 |
| --- | --- | ---: | ---: | ---: |
| TH | base | 5 | 0 | 0 |
| TH | r8 | 4 | 1 | 0 |
| VI | base | 3 | 0 | 2 |
| VI | r8 | 4 | 0 | 1 |

- **D03-B = r8 / TH / seed44**，用户标「有明确异常；模糊」；
  D03-A为同条件base，标无明显异常。两句为 `fleurs_test_th_1739 → fleurs_test_th_1707`，
  ref为 `ref_tl_03.wav`，CFG1.8/20步/strength1/禁重试。
  这是**双句组级声学异常确认**，缺少具体句与时间点；不能据此认定娃娃音、金属音、
  发音错误或把两句各记一次已确认坏例。
- D07-A/base与D07-B/r8均为VI/seed43，均「不确定；有些模糊」；
  D09-A/base为VI/seed45，标不确定，D09-B/r8标无明显异常。
  不确定全部保留，不算正常或失败；尤其不能把D09计为r8已修复。
- 合计16条无明显异常、1条明确异常、3条不确定；8对两侧判断均明确，其中1对r8有异常/base无明显异常，
  另7对两侧均无明显异常。后者不等于7次自然度平局；**自然度10对全部未评，退化结论为未验证**。
  样本只有4条文本、2个ref，且按诊断结果富集，不能估计线上坏例率或声明五语种验收通过。
- 音频来自本项目裸输出的保真拼接，没有经过OmniVoice/DIS后处理。
  因此本条反馈不能作为生产后处理引入模糊的证据；先回两句原音频/ref定位，
  不先扫描更多WavLM窗口或重训。没有根据这1条反馈改配比、加epoch或调整全局CFG/步数。
- `eval_cases/dual_cue_regression_20260917.jsonl` 保存这两条**开发回归成员**及组级反馈来源，
  明确单句尚未确认。复现时各句独立生成后按原顺序拼接，不能改成一次长文本生成；
  优先复用现有WAV，不改变原150条留出集或原自动评分。

导入复用 `eval.save_reviews`，仅填写 `acoustic_status` 与原文备注；
未从「模糊」自动推断异常类型、`noise/cutoff` 或母语分数。
回归检查覆盖错试听包、未知/重复代号拒绝、按模型归位、不确定保留、原输入不变与拒绝重复覆盖。
本地相关5项及全套140项通过；额外核对20条实际反馈、两条回归成员的原文/ref/seed和完整PCM。
产物：`feedback_review/{submitted.txt,mapped_feedback.json,summary.json,reviewed_base.json,reviewed_r8.json}`。
带标注副本的 `report_path` / `paired_report` 为本机路径。导入当轮SSH端口探测未通过，
当时没有执行同步或启动GPU任务。后续连接已恢复，新增产物/代码文档已补同步，
仅作归档，不冒称已写入6006报告列表；没有丢弃标注或重跑合成。

SHA256：
- 原文：`0c5481a6c7df22f1c57284b1313dc0cc513fe5d87d63fdc23357f50b58f099a0`
- 对齐反馈：`f0b1dc44c263ab150c4ab5104feaf122c37cb4b7a9e782fd93207349358a3736`
- 汇总：`23bf09831bdbbf1259cc008b890bf112c41d290cca023afe5131ebad8ac62c8f`

## D03续查：原单句未被拼接改音，ref来源已核对

SSH恢复后先保存远端待同步文件快照至本机 `sync_recovery/pre_sync/`，确认差异仅为上轮反馈追加，
再定向同步代码、文档、回归清单和离线反馈；没有覆盖远端整个工作区或改其Git历史。
远端Git仍在旧提交且靠文件同步，不把它的HEAD当实际运行源码版本；没有重启6006或模型。

只从远端取4条原单句及1条共用ref，另建 `dual_cue/d03_localization/`：

| 补听代号 | 模型/原case | 时长 | 原双句中的区间 |
| --- | --- | ---: | --- |
| D03-A-1 | base / TH1739 | 7.52s | D03-A 0.00–7.52s |
| D03-B-1 | r8 / TH1739 | 6.88s | D03-B 0.00–6.88s |
| D03-A-2 | base / TH1707 | 7.52s | D03-A 8.02–15.54s |
| D03-B-2 | r8 / TH1707 | 8.96s | D03-B 7.38–16.34s |

所有单句为48kHz/mono/PCM16；文件SHA256与冻结组件记录一致，PCM与双句相应区间逐样本一致。
间隔严格为24000个零样本，没有交叉淡化、响度处理、变速、截词或重编码。
四条单句及ref均无PCM16满幅样本；这只排除本文件的满幅样本，不排除模型内失真、录音已有失真、
听感模糊或播放器/设备影响，也不等于判断拼接边界听感正常。

参考音 `ref_tl_03.wav` 为16kHz/mono/PCM16、5.11秒，SHA256
`c749ef6d945bb1c1c95f58bdad82877846ee76f3fbf4e20334730d1009ca72e8`。
在远端 `fleurs_tl_tc/val.jsonl` 的38条中有1条完整文件哈希匹配：
`audio/run_xiuv65q7/0001013.wav`，原始追溯 `raw/fleurs_tl/audio/0001013.wav`，
文本为 `Ang lahat ng mga mamamayan ng Lungsod ng Vatican ay mga Romano Katoliko.`。
源清单SHA256 `cf52228ea2e76dcedcf793968a41918281c44c99a628c81b68d8045c8f9bd8ab`。
`speaker=fleurs_tl:unknown`、`speaker_verified=false`；来源匹配不等于音质合格、同人身份已知或无训练泄漏。
本次不重新审计所有训练曝光，也不拿另一条TL参考音冒充同人替代。

复用原报告、不重跑ASR/频谱/音色指标：

| case / seed44 | CER base→r8 | 2–8k能量占比 base→r8 | 原有疑似漏尾 |
| --- | --- | --- | --- |
| TH1739 | .0515→.1237 | .0266→.0279 | 两侧false |
| TH1707 | .0865→.0865 | .0771→.1292 | 两侧true |

两句两侧原有`metallic=false`、`low_snr=false`，但不能推翻用户确认的组级模糊。
第一句CER变差不能直接证明其发音错误或定位模糊；第二句含 `(SANParks)`，
ASR的漏尾提示不能替用户裁定真实截断。频谱也不是“模糊检测器”，没有统一变暗的数值证据。
同ref、同两句的另4个seed均被用户标无明显异常，故不能归结为“这条ref必坏”；
仍可能存在模型/文本/seed/ref交互，不作单因果结论。

本机入口 `checkpoints/holdout_eval_20260917/dual_cue/d03_localization/index.html`：
只用原生播放器，相邻 `audio/` 供离线播放；D03-A/B保持原映射，1/2表示句序，D03-R表示ref。
这是可选补听，不要求重评10组或提供时间点。导出时单句未评，后续反馈见下节；原反馈不自动拆给两句。
`verify.py`为只读完整性检查，输出`audit.json`，含文件/PCM/页面/源码哈希及复用的原诊断字段。
执行 `uv run python checkpoints/holdout_eval_20260917/dual_cue/d03_localization/verify.py` 可复查；
不改输入、无需网络/GPU、不生成音频。页面相对路径、代号/原文件对应及5个WAV完整读取已验证，
导出时未冒称人工补听完成。项目本地140项、远端相关33项测试通过。

生产链路只读核查见 `docs/inference_chain_audit.md`。工程核对已排除文件下载或拼接改变PCM，
未据此定位模糊或宣布修复。后续听评与当前决策如下；实际模型身份与最终参数追踪独立推进。

## D03补听结果：原单句与参考音均无明显异常

用户后续原文：「试听了，都没有异常」。按紧接上一条给出的补听页解释范围：
`D03-A-1`、`D03-B-1`、`D03-A-2`、`D03-B-2`及`D03-R`均记录为无明显声学异常。
即4条生成单句与1条参考音，不把参考音混算成第5条模型输出。
原文、范围解释、逐代号判断和冻结页面/映射哈希另存
`dual_cue/d03_localization/feedback.json`，没有覆盖原单句报告、双句反馈副本、WAV或网页。

- 初次双句D03-B的「有明确异常；模糊」保留；本轮单句/ref未复现明确异常。
  原双句连听没有报告再次评审，不把此次反馈改写成原双句已通过，也不擅自撤销初次观察。
- 两次试听没有改音频或模型/参数，因此不能记为“修复成功”或调参收益。
  连听上下文、听感波动等原因均未验证，不强作解释。
- D03暂不作为稳定可复现坏例，退出当前优先修复项；两条开发回归成员仍保留，
  只追加复听状态，不放回未使用留出集、不当训练样本或已确认单句坏例。
- 本次不改变初次20项反馈统计，VI不确定项仍不确定；自然度、发音、口音与情绪仍未验证。
  不要求用户继续重听，不开r11或新增参数网格。下一步优先实际模型身份和最终采样参数追踪。

## 产物与下一步

远端 `/root/autodl-tmp/holdout_eval_20260917/`：
`base_report.json`、`r8_report.json`、`comparison.json`、`voice_stability/`、
`diagnostic_sample/{plan,reports}.json`、`asr_recheck/results.json`、
`asr_repeatability/{plan,results,status}.json`、`asr_alignment/results.json`、
`asr_repeatability_random/{plan,results,status}.json`、
`local_voice/{plan,results}.json`、`local_voice/embeddings.npz`、
`voice_windows/{plan,results,reports}.json`、`voice_windows/{source.py,embeddings.npz}`、
`voice_windows.log`、`dual_cue/{plan,results,reports,verification}.json`、
`dual_cue/source.py`、`dual_cue.log`。
拼接WAV另在 `/root/autodl-tmp/voxft_ckpt/eval/dual_cue_audio_65580ec49a3b49028276274f75092017/`，
两份双cue报告在同一 `eval/` 目录，不将这些衍生试听音频放入真实数据源或训练清单。
完整评测源码快照不覆盖；诊断导出与ASR复核的追加脚本分别留版本快照。
本地仅备份报告/元数据/源码及本次小规模嵌入，不下载训练数据或源音频。
新增重复性完整性/重复记录检查与词级对齐单变量/不覆盖检查均通过；
本次新增固定抽样分层/不重选检查、裁边单变量/原样复现/不改源音频检查，
以及窗口边界、复用嵌入、拒绝已变更输入、原始完整cue导出、双cue拼接/盲化/仅供试听守卫检查；
本地全套 **139项通过**，远端本次相关 **32项通过**（诊断4项与既有eval/质量实验测试28项）。
此前本地138项/137项/135项/133项及远端相关6项/5项/2项/28项记录保留，不把远端局部测试冒称全套。
上述追加实验均已结束，输出目录拒绝覆盖，不要重复启动。

追加结果SHA256：
- `asr_repeatability/results.json`：`cce5b43d3118f730d0ac3c271b178fd49825710f1ea7dddc1dd175f604d582c9`
- `asr_alignment/results.json`：`64b8e4777165fece9d61c650d9ea03566dd64bb2ac535481202f0900431aedcc`
- `asr_repeatability_random/results.json`：`9f7e16841da9bf669ce6a108c3a560e303b4d26306fee671ed876e49d43d6706`
- `local_voice/results.json`：`a0076339499dd34f0ff3f3bf37c5fe076270002be340fb2f5bd5d56f859d7856`
- `voice_windows/results.json`：`0e4ea0251b77181f7fc1ec6ac8a07b1f6232325ab175bbed6e15230ac8c3b681`
- `voice_windows/embeddings.npz`：`599120bf7dce4a2dc75e1265e38dfb555ee2d17b2838b6e6e9e0e078ca7f00e9`
- `dual_cue/results.json`：`ef4a3c1debe1edd6b8b4b716293c57e122b3725ca0414ae871f62758e9859af3`
- `dual_cue/verification.json`：`14b356de4e59203ea7938b943f6e3a7278ab388c10b291151358301ef36b8bbf`

1. **保持r8，不扩大r10、不启动r11**。ID/MS有内容改善信号；其余内容结论不明确，
   音色组均值略升但仍有局部下降，不以均值掩盖问题，也不推翻已有生产短cue诊断。
2. 固定随机ASR复核已完成，暂不扩全量、不改默认、不覆盖原评分；
   VI重复转写若继续归因，需要实际片段或独立识别证据，不能靠同一ASR给自己背书。
3. 三位置定位、组内甲乙固定的双cue材料已完成，停止扩大同一指标扫描。
   D03初次双句反馈模糊，后续4条原句/ref补听均无明显异常，不作为稳定坏例继续扩查；
   VI43两侧仍不确定，不升级为已确认异常。下一步优先实际模型身份与最终参数追踪，
   另核对ref与生产后处理的证据链：
   只有同text/ref/seed/实际cfg/steps的裸输出→处理后音频
   才能作阶段归因；已有归档配不齐时记录缺口，不把不同条件的差异解释成LoRA或后处理缺陷。
   没有独立声学证据就保留疑似，不再用更多WavLM窗口替代确认。
4. 只有可复现问题、明确改进目标和已审核干预数据同时具备，才设计同预算单变量r11。
   当前没有据此指定新数据比例。自然度/口音/情绪仍未验证。
