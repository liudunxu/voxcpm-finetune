# 2026-09-20：TH原声回退、MS成片与是否微调

## 结论

**保持r8，暂不重开微调。** 已明确的问题优先在选择/重试/混音链修复；
MS片的参考污染与时间窗问题也不能靠训练替代。正确输入下仍有稳定裸TTS坏例，
再冻结同条件base/r8局部对照，之后决定数据或LoRA实验。
这不是模型质量全面达标，也不是不再训练；本轮没有新GPU/ASR/TTS或部署操作。

实现与完整白名单复盘：
`../../dubbing_intelligence_service/docs/reviews/2026-09-20-best-tts-and-ms-review.md`。

## TH：23秒原声已定位

- 视频尾名 `mtc4l3g7puyr55`，meta为2026-09-20北京时间15:11:37。
- cue3为23.519–26.500秒，译文存在；Qwen/Whisper均转出额外否定词，
  最终 `critical_mismatch` 后旧混音回退源人声，不是漏翻译。
  双ASR一致不等于已确认实际念错，仍可能受共同语音混淆影响。
- 末两次请求同seed1761913275/CFG1.5/28步，最后一次generation_attempts=0：
  “修复”其实命中缓存。DIS本地改为证据/预算允许时真换seed，不新增次数或换参考。
- 用户最新策略是最佳可用TTS出片。原QC失败证据保留、标待复核，不续写到邻cue；
  无可用TTS不自动混原声，显式笑声不变。旧文档的“一律丢弃失败TTS”不再是当前产品策略。
- 同时修复critical候选同分无法择优、重试无音频反而压过旧候选的问题。
- DIS相关458项回归通过；旧源码快照102项中15失败、87通过，
  含真实ffmpeg失败对白静音/显式笑声保留检查。修复已随DIS功能提交`79b7af4`提交，
  保留远端6条提交后以`3dc9477`推送；合并后343项相关回归通过，未部署或重渲。

## MS：不是PH/TL质量评测

新片 `ph-11110136-6v3u0-mtc4nbj347wj6c.mp4`，86.192秒；
SHA256 `b29a19684a78177eda51f0e72d23c9af8d426e7fb859040abf2d9823c9d7f636`。
meta为2026-09-20北京时间15:25:39，目标 **ms**，源配置auto且源文英语。
13条合成+1条显式笑声，零tts_failed。没有母语听评，不声称音质/口音通过。

- **识别/参考优先**：cue12（65.75秒）参考有混声/噪声，cue14（77.291秒）有音乐/噪声；
  无可用替代后仍带风险出片。先取得源声核对边界与同人参考，不盲降门槛或借同名人物。
- **合成链路核对**：cue7（28.208秒）仍有speaker_mismatch、两ASR冲突；
  cue8（30.625秒）仍有output_vocalization，正文ASR pass没有解除声学问题。
- **翻译/时序核对**：cue1/2/11/13实际结束比字幕窗尾约晚2.940/1.346/1.350/1.661秒，
  都被扩展安全窗接纳。需对照源语音节奏，不能把“不覆盖下一cue”当同步通过，也不应硬裁尾字。
- 同角色输出有风险，不足以直接归因模型。cue8等为独白链，不能仅凭显示同名合并；
  客户端meta写基座地址不证明服务端实际权重，尚缺权重SHA与独立ref/take。

冻结证据及白名单：
`checkpoints/video_review_20260920_mtc4l3g/`、
`checkpoints/video_review_20260920_mtc4nbj/{feedback_video.mp4,dubbing_meta.json,summary.json}`，
均不提交音频/完整meta。

## 审核页27/90的独立边界

用户要求查看 `/dubbing/review/27/90`。静态页面HTTP200；
页面使用的只读 `/dubbing/api/dramas/27/episodes/90/review` 为HTTP401，
当时没有可用已登录浏览器，未能读取该页项目数据。
随后已通过用户导出取得审核JSON并取回对应源片/成片：第13条8.16秒是源窗口，
不是两词TTS时长；本地共享链路保护及局部源ASR诊断见`video_review_20260920_vn_27_90.md`。
不再等待登录，不拿上述MS片冒充该页评估；本节后续有一次局部源ASR，未重渲或训练。
