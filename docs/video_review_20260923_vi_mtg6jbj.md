# 新VI成片：漏对白、短句越窗与重复沙哑

## 证据与版本

`id-11110136-6v3u0-mtg6jbj9msqp70.mp4`，metadata目标语`vi`，136.24s、28cue；
视频SHA256=`ff4078a215a533e908e2f24fa8e8ecc32e49c24911573749f0dfc3a12e9400c8`。
用户提供18–20s原声“妈的”、24–27s声音异常/“脚本声”、1:16及1:53沙哑反馈。
“脚本声”可能指脚步声，未替用户确认；没有从文字/频谱声称亲耳试听或完成母语验收。

本地ignored证据：`work/video_review/20260923_mtg6jbj/`；复用DIS工具恢复17个按SHA去重的
客户端ref/prompt，4,004,328字节。未取得此片实际raw/segment、源stem、原识别ASR日志或GPU编码器输入。
项目31/集96/script revision68。输入未提供recognition_task_id；不能把旧脚本沿用说成已重新识别。

publisher与22个TTS已采纳请求的DIS源码指纹均为
`cf37a2d5c1a8b20bac0403e26e47325d0722ce3fafd55932700ad444487a1ef3`，
匹配本地修改前`bd2ada7`包内容。**这轮6次严重风险共享预算确已生效，不能再用“尚未部署”解释残留。**
但不证明所有worker、OmniVoice运行源码或r8实际权重SHA；模型身份仍未完整核验。

## 各反馈不混成一个病因

| 用户位置 | 已有证据 | 判断边界 |
|---|---|---|
| 18–20s | cue5 16.38–17.853、cue6 19.605–22.144均为`(wailing)`且keep_original；没有这句对白 | 附近确有非词发声原声保留，不是TTS失败回退。报告范围还包含17.853–19.605的无cue空隙；精确词起点/背景stem泄漏及ASR漏词、后续改标的区别还需源证据 |
| 24–27s | cue7目标`Đi đi.`，22.28–23.48字幕窗，裸输出4.48s，segment4.202313s、placement0 | TTS实际覆盖22.28–26.482313s；这段没有独立事件cue。不能只认定脚步被误识别，先排短句过度生成/错位；语音包络late3.285s只是能量观测 |
| 1:16 | cue15 `Để tôi ra ngoài!`，74.6s起，segment到76.1s；仍speaker_mismatch/unresolved | 用户明确沙哑；不是文本pass就算修复，最终ASR pass仅支持文本覆盖 |
| 1:53 | cue25 `Hôm nay tốt nhất hãy giết tôi đi.`，111.68s起、placement0.17s、segment1.78s | 用户明确沙哑；已采纳take无control，仍harsh_high_freq/plosive/text_incomplete。不能仅归因为情绪 |

cue7没有硬裁，atempo1.067；`content_fit.complete=true`表示装进含尾部余量的4.2秒容器，
**不等于1.2秒短台词的发声时序正确**。最终文本QC uncertain；不按弱Whisper时间戳直接裁掉前三秒。
keep-best保留TTS符合用户不回退原声的要求，但不是质量通过。

## 沙哑风险簇与预算

cue15/25以及此前1:57的cue27都用了同一客户端参考：
`315f69f37a30ec92576f8686d69f1177d1b68a1f3a231074143b627978e604cb`，
4.14s/32kHz mono，episode canonical锚点13/27。本片嵌入字节、实际请求SHA已核验。
参考SNR1.52dB、Gasp风险是排查线索，不证明沙哑病因或参考已干净；未对本条参考取得新的人工听评。
首句旧病例的长前导低能量不能外推到这条参考；低SNR也不自动给它贴“坏参考”真值。

- cue15：已记录4次HTTP候选，服务端生成计数2+1+1+1=5；最终有效CFG1.5/22步、seed709426392。
  两次后段reference-only请求除了max_generation_attempts3→2，其余记录参数/ref/prompt相同，
  均seed2093423177、返回音频SHA相同；**不当成两条独立探索或两次改善**。
  这暴露出跨救援回滚后的同条件重复请求机会，尚未修改seed策略或宣称省掉实际GPU耗时。
- cue25：1+2+3=6次服务端生成，最终有效CFG1.5/28步、seed455751961、control空。
  无控制救援仍失败，不再简单全局关情绪或继续堆相同参考重试。
- 本片cue27：2+2+2=6次，reference-only已执行仍留output_vocalization；这是新策略残留，
  但用户本轮没有再确认它的听感，不能计为新的人工投票。

新旧片对比：22个TTS cue中11个raw SHA相同、11个改变；cue15改变、cue25相同。
本片`tts_cache_reused=true`也用于第二轮装配，不能据22个标志推断“全部旧TTS”。
相同SHA可能来自缓存或确定性生成，仅凭现有字段不区分。两个新坏文本共享同一角色/ref，
是一个风险簇，不当作两个独立身份样本。

## 已落地：补识别不再跳过整个长空隙

审查DIS现有`dialogue_recovery.select_windows`发现只接受0.6–5s的整段空隙，长空隙直接略过。
本片相邻对白12.537→22.28约9.743s，若原自动字幕存在这一空隙，旧规则无法覆盖；
**缺少原识别记录，不能把这个条件推断写成已证实本片唯一根因。**

最小修复`gap-dialogue-v3`：

- 长空隙按5s独立归属窗扫描活动，每次送ASR含上下文仍≤6s；能量只挑窗口，不判定有人说话。
- 仍每集最多4窗、每窗最多3次ASR、该补识别阶段最多12次/60s；没有全片双ASR或扩大正常首轮。
- 接纳仍需两个ASR的完整词时间、文本/时间一致和邻句去重；上下文词不注入，跨窗词不截断。
- 分窗争议保留needs_review，不交给假设整段邻句关系的旧joint-boundary救援，以免跨窗重复/改邻句。
- 非词发声文字候选提前停止第二ASR，仍标未确认，不据“啊啊”或ASR空结果认证wailing。
- 更新策略版本使开启该功能时旧缓存失效；开关仍Config Center的`recognition_config.gap_dialogue_recovery_enabled`，
  默认false、不偷偷全局开启；本片是否开启未知。用户上传/已锁脚本不在这条自动补字幕路径内。

旧代码新增4项回归全失败；修后10个相关文件**483通过（7.19s）**。未重新识别/重渲本片，
不以单测证明“妈的”已恢复，不外推生产耗时，也未修改事件检测器来强行放行/删除某一台词。

## 下一步与r8

1. **识别链**：取得原ASR/脚本及stem，灰度开启有界补识别，重新识别而不只点重渲。
   用短对白、粗口、真哭笑、脚步/打斗负例验漏检/幻觉/邻句重复；事件分类不能单独证明“没有词”。
   不确定区间显式复核；不能直接把既有wailing字符串改成台词却保留keepOriginal标记。
2. **合成链**：优先验证共同参考与实际GPU条件；避免同条件重复救援，保留风险与阶段音频。
   当前未再扩4→6上限、不跨角色借参考、不提高全局步数/CFG、不对长take按未知内容硬裁。
   同日已修共享重试seed：在原cue/attempt/reason派生中加入累计请求序号，避免失败回滚或缓存
   恢复旧attempt后重复探索；首take/软风险锁seed不改。仅本地测试，不声称新听感已改善。
3. **模型**：值得准备定向提升，不再笼统说“无需微调”。原两例12条计划不动；
   新1:16/1:53追加独立12条base/r8计划`eval_cases/roughness_model_ab_20260923_mtg6jbj.json`，
   同参考/文本/各自有效CFG/步数/control，原seed/+1/+7，禁重试。原12与新增12均**0生成**。
   执行器复用`scripts/roughness_ab.py`，需要独立Linux GPU和基座/r8 SHA核验；不热切生产模型。
4. r8更差先验证基座/LoRA强度，不直接续训；可靠输入下两者均稳定失败，才用核验真人短句、
   同人ref做单变量训练实验。r8历史训练ref/control为0是待检假设，不是克隆失效证明；
   r10/r11没有晋级收益，不重复它们的配方。少量VI不替代五语种分开验收。
5. 用户要求的`work/for_train`先备料，详见`docs/training_material_pool_20260923.md`；
   成片、合成正例、未知授权短剧和历史测试波形不进训练集，已有评测文本加入排除清单。

新1:16/1:53反馈冻结于`eval_cases/production_vi_mtg6jbj_feedback_20260923.jsonl`。
追加诊断输入已备好`work/audio_ab/20260923_model_attribution_mtg6jbj.tar.gz`（184,450字节）；
共用ref与本地r8三文件SHA通过，独立基座文件与Linux执行通道仍待核，不等于已完成全量check-only。
语言质量仍未验证，未开始微调、未替换r8、未新增生产视频。
