# TH 新片复盘：参考音可取回，漏句恢复，短句风险未解除

## 证据与边界

- 文件：`th-11110136-6v3u0-mtf4ok1mvzlvf9.mp4`，SHA256
  `d6ff1b674888a40c861382aa23fe5b1b97e24f36803a6f8385d7b7e7d81eb741`。
- 86.843991秒，27,963,626字节；meta源语en、目标th，源字幕为英文。
  发布快照时间为2026-09-22 18:15:31北京时间。
- 对照前次G06 `mterel323itd83.mp4`：23条cue的起止时间、目标文本完全一致；
  编码画面包内容及PTS/DTS/时长的联合SHA均为
  `a23eb626bbb1c507eae8a25c12b84259f5e3e7e258683ff57ec7c9694fa2e824`。
  可以按相同时间定位，但生成候选/参数不同，不是单变量模型A/B。
- 首次复盘是元数据、文件与源码核验，未实际听音；下文初始风险不是人耳确认的异常。
  后续已收到用户完整7条声学反馈，见文末；语言质量仍未验证。没有新TTS/ASR、训练、模型切换或部署。

## 待QC参考音已实际取回

复用DIS的`inspect_dubbed_video_metadata`和`extract_review_references`，从单个MP4提取
19个去重WAV，共3,101,706字节（约2.96MiB）。15条TTS的reference及7条使用中的prompt，
共22个关联全部通过文件SHA及客户端请求hash匹配。未使用prompt明确标记，不补造。
这些是客户端输入字节，不是GPU去混响/降噪后的内部参考，不证明真人来源或训练权利。

视频自身仍不含裸TTS、完整历史候选或所有最终segment，需要实际任务的`CUE_AUDIO_BUNDLE`
才能补齐同take的处理阶段对照。不猜内部下载路径，不要求从已销毁的旧容器取文件。
新文件比旧片大约3.37MB，但同时改变了音轨/诊断，**这个文件差不能全算成参考音净增量**。

本片发布进程源码SHA为`9adcde15a4aa2989bfe51842c6db1349d5429eba5dffc43cacbb5a5944192cda`，
与当前本地源码摘要不同；内嵌git commit为空、权重状态unverified。
能证明此产物已有参考归档及非空片段，不能据此认证完整部署版本或GPU权重。

## 逐项结论

全片15条TTS均有非空最终时长（旧片14条，cue4失败）；另外8条为显式保留的笑声/哀叫，
不是TTS失败后回退源语言对白。新旧片均有cue19/20/23带severe；不能称为整片QC通过。
新版最终文本QC为10条pass、2条pass_forced_language、2条uncertain、1条未记录。

| 时间 / cue | 已核事实 | 处理优先级 |
|---|---|---|
| 22.32s / 4 | 旧片后处理空WAV；新片2.735s原take→1.345646s segment，最终文本pass。两版客户端返回音频SHA相同，确实是同一返回take的不同后处理结果 | 漏句工程故障已恢复；仍1.3倍速、溢出约0.066s，不能宣称完整语言验收 |
| 58.00s / 19 | `วิ่ง! วิ่ง!`；severe为额外发声、speaker_mismatch、text_incomplete，最终文本uncertain。Qwen转为`Ring ring`，Whisper指定th为`วิ้ง`；本地输出/参考F0中位400/190.5Hz | 首要试听点；ASR/F0只说明风险，不能自动认证串语种或女声 |
| 59.80s / 20 | 旧选中16s→新选中1.22s，仍硬裁成0.88s。裁后复核已执行、最终forced pass，但`complete=false`与severe text_incomplete保留 | 不能把forced pass当成完整通过，优先检查实际截尾和QC状态聚合 |
| 18.80s / 3；42.92s / 14 | 参考存在自动gender mismatch/dirty-reference记录；同speaker其他cue实际使用不同reference hash且未锁身份 | 先核源身份与参考选择，不凭性别分类器或F0认定真人性别，不先归因LoRA |
| 53.08s / 17 | 极短人名，音区漂移风险；Qwen coverage .333、Whisper .667后forced pass | 保留声学与超短句识别不确定性；不要求用户判断泰语人名是否正确 |
| 70.12s / 23 | 15.73s原take→16.548254s segment，tempo .946；文本pass但output_vocalization severe仍在，未硬裁 | 检查额外发声；仅约5.7%的时长拉伸不足以证明主观“过慢”，不全局改速 |

### 58秒候选选择需审核，而非简单按分高替换

同speaker的cue2/19/23使用同一7.54s客户端canonical参考，但cue19另带2.6s prompt，
cue23走有控制前缀的reference-only；不能把不同模式当作相同条件模型失败。
cue19还生成了一个reference-only候选，最终评分从-48.5到27.8，但仍有text_incomplete，未采纳。
源码`should_accept_adjacent_timbre_resynthesis`要求`retry_problem`为空后才比较分数，
因此不是单纯算分失灵。后续应拿双方裸音与完整风险验证“已有风险时的相对最佳”是否应放行，
不能只凭高分删除内容保护，也不能为验证这个问题重跑全片。

### 59.8秒refit没有真正执行

meta为`requested_indices=[4,20]`、`status=no_eligible_cues`。
当前共用collector要求源文与译文均存在、溢出至少.15s：cue4只有.066s，cue20源文为空。
两者与未入选结果一致，不是refit已经缩译成功，也不是可直接将阈值清零的依据。
cue20裁后Qwen为`Oh alright`，Whisper为`โอ้ อร่อยน่ะ`；后者compact coverage仅.455，
不拿宽松对齐coverage=1认定完整原句。总体severe仍被保留，当前不能称全局风险已被洗掉。
下一步优先追溯cue20源文缺失及短句时间窗，复核裁后ASR不一致的状态，不新增全片双ASR。

## 本地定位页与后续顺序

DIS：`work/qc_review_20260922_mtf4ok1/index.html`，试听包`th-ref-mtf4ok1`。
T01=58s，T02=59.8s，T03=18.8s，T04=42.9s，T05=53.1s，T06=70.1s，T07=22.3s。
每条提供成片混音、实际客户端reference及可用prompt；**不是裸TTS/模型A/B**，
差异可能包含背景音和处理链，不把参考/成片的主观差异直接作为模型因果证据。
初次请用户优先听T01/T02；现已收到全部7条反馈，不再要求重复填同一份定位包。

1. 优先定位58–61秒的输入、候选选择及裁后结果；拿得到真实任务音频包后补raw/segment。
2. 对参考可用性与身份链做单点审核，不跨不明身份借音，不全局换参考或关闭降噪。
3. 确认可复现裸TTS问题且排除上述混淆后，再考虑固定输入的局部base/r8或推理参数A/B。
   本片不足以支持重开微调，不增加正常cue的GPU请求。

证据在本项目`work/video_review/20260922_mtf4ok1/`（忽略目录，不能从git恢复）。
`review.py`复用已提取材料生成页面，包含22关联SHA校验、画面包对照、7个非空WAV检查与JS语法检查；
`verification.json`保存核验结果。页面媒体路径另经本地检查；未声称浏览器播放或听感验收通过。

## 用户完整听评与处置（2026-09-22）

完整反馈覆盖此前仅T01/T02的提交，计作同一轮，不累计成两票。原始字段保存在本地
`human_feedback.json`；两个明确成片坏例登记于`eval_cases/production_th_reference_feedback_20260922.jsonl`。

| 代号 / cue | 成片 | 参考 | 用户备注 |
|---|---|---|---|
| T01 / 19 | 无明显异常 | 无明显异常 | 未评 |
| T02 / 20 | 有明确异常 | 不确定 | 参考音包含两个人的声音，比较急促 |
| T03 / 3 | 无明显异常 | 无明显异常 | 未评 |
| T04 / 14 | 无明显异常 | 无明显异常 | 未评 |
| T05 / 17 | 无明显异常 | 无明显异常 | 未评 |
| T06 / 23 | 有明确异常 | 无明显异常 | 成片有些杂音 |
| T07 / 4 | 无明显异常 | 无明显异常 | 未评 |

**优先级改为T02、T06**。其余5条降声学排查优先级，但不认证自动QC误报，不清除文本或身份风险。
T02参考评级仍为不确定，多人声/急促备注单独保留；T06只确认为混音杂音，不擅自归类爆音、金属音或模型故障。

### T02：发现可复现的边界漏洞，尚未证明完整因果

本片源窗由0.8s扩为`59.4–60.7s`，实际客户端参考1.53s，含0.2s补静音。
记录的尾padding约0.03s会越过下一条60.7s的笑声边界。参考QC报speaker_count=1，
但`reference_speaker_check={}`不是独立同人验证；现有自洽声纹检查最低3s，1.53s即使送检也只会
返回too_short，不应为这条把门槛调低到不可靠的短嵌入，也不新增全片多人声请求。

本地已复现两处共享代码问题：
1. 扩参考窗只更新source起止，沿用扩窗前padding，重新侵入邻人区间。
2. VAD交集再补50ms可以越出调用方给定的参考窗；自动选参考和渲染共用此入口。

最小修复：扩窗后重算padding且仅收紧；VAD补边限制在原输入窗内。
显式零padding保持不变，不改字幕/成片时序、模型参数、重试预算，也不添加ASR/TTS请求。
旧代码新增回归2失败2通过；修后连同参考选择等8个测试文件共123项通过（7.79秒）。
补丁尚未push/部署，未做修复后效果A/B，不声称用户已确认听感改善。
这只证明边界漏洞，不证明“两个人”都来自那30ms；原始粗时间窗内部可能已混人，仍需真实源窗证据。
已有归档音频不改、不拿修正后的参考假装同一次请求；只重建参考并生成新输入时才会应用新边界。

### T06：按处理阶段分离，不先开训

本地高频刺耳标记false、文本QC pass均不反驳用户杂音反馈；参考与T01共用同一SHA，
但两个cue的文本、控制与prompt模式不同，不是单变量对照。
meta显示原take峰值约-0.54dBFS、段级limiter曾启用、tempo=.946，后续响度匹配再降约5.26dB，
最后还有背景混音。不能因“后续响度匹配limiter=false”误报整条没有限幅。
这些是排查路径，不是噪声成因证明；尚未取得raw/segment，不全局关降噪、限幅或变速。
下一步先取该任务真实音频包，对同take的raw→segment→mix定位噪声首次出现在哪层，
只有拿到实际替代方案再发局部A/B，不让用户重复评价当前已确认的坏例。
